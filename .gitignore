node_modules/
local.db
local.db-*
*.db
*.db-journal
*.db-wal
*.db-shm
.dev.vars
.wrangler/
dist/

# Reproducible collector output (regenerated by scripts/collect-pokemon-vending.mjs) — not source.
data/pokemon-vending-import.json
loops/site-redesign/render/

# QA-suite screenshot output (scripts/qa-*.mjs regenerate these on every test-all run) — not source.
# The tracked copies were deliberately dropped twice (eab62dc, c686b4f); keep them out for good.
loops/site-redesign/proofs/

# Playwright launch-gate artifacts
test-results/
playwright-report/

# Per-task gate unlocks (REBUILD_PLAN) — absence is the permanent default; never commit these.
.unlock
.unlock-bg
.unlock-voice
