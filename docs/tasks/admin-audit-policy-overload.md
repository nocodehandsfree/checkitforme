# Policy page is overloaded — split per comp, drop duplicated blocks

**What:** The comp says Policy splits into a CONSOLE (flags) + LOG queues (requests/waitlist/
moderation). The code does that split, then bolts on a pricing form, a plans mirror, a Pulse block,
and an intel block — overloading one page. Pulse duplicates Live's Members, intel duplicates the
Restock page, and kiosks duplicate the Kiosk tab. Trim to the comp; remove the duplicated blocks.
**Done when:** Policy matches its comp (flags console + queues), with pricing where it belongs and no
Pulse/intel/kiosk duplication of other pages.
**Lane:** Addie
**Tag:** comp
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
