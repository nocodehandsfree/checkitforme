# Self-improving Charlie — the build (owner's go 08-16)

**What.** Build the six stages of `docs/specs/self-improving-charlie/README.md` (THE BUILD PLAN
section), one stage per session, in order. The spec carries every owner ruling of 08-16: the
08-07 money-in-tests reversal, the money brake (20/50/100/200, approved), the measured simulation
usage unit, benchmarks deferred until the floors are computed.

**Done-when.** All six stages live on staging and driven; the "How we will know it works" list in
the spec passes end to end; the trust ladder is at stage the owner has ruled.

**System.** voice-calls (stage 2 touches admin).

**Status.** Stage 1 DONE 08-16 (the grade sees waste — meter.ts, card bounds, sheet, test-meter;
on branch claude/self-improving-charlie-2gq2xg, PR #121, awaiting a merge window the owner calls,
since a staging push kills a live check). Stages 2 to 6 open; next box = stage 2, every customer
check graded and filed in Admin under Calls.
