# Pull 424 from staging ADMIN_PHONES before the real-card walk

**What:** The owner's 424 number is in staging's ADMIN_PHONES Railway var, so a fresh-customer signup test boots as admin. Pull it, verify clean signup. Prod comp already undone — do NOT re-comp 424.
**Done when:** Fresh signup with 424 on staging boots as a normal customer.
**Lane:** Ops
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
