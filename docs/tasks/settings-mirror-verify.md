# Settings mirror: verify prod export live

**What:** The settings-sync promote landed. Verify GET /api/settings-sync/status is clean on prod and a real Admin edit reaches staging within a minute.
**Done when:** Status endpoint clean + one Admin edit observed arriving on staging.
**Lane:** Ops
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
