# Policy flag labels should use real feature names

**What:** The Policy feature-flag labels don't all match the site's real feature names (the audit's
example: the score-posting feature is "Post Your Score" on the site, not a made-up flag word). Reconcile
every flag label in Policy against the real consumer feature name it controls, so the owner toggles by
the name he actually knows.
**Done when:** Each Policy flag reads as its real customer-facing feature name; no invented flag words.
**Lane:** Webbie
**Tag:** copy
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
