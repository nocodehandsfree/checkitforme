# THE ORDER — what merges when, 08-02 (PM handoff)

Six agents have work in flight and the owner needs one queue. **ONE merge at a time. Whoever merges
pulls staging first.** Everything below was verified against the real branches on 08-02, not assumed.

## Already landed, nothing to do
The robot store · the chain logo code · Addie's statuses fixes (three commits on staging).

## THE QUEUE

**1. LOGO AGENT — push now.** Smallest, independent, gets one thing off the board. Nothing else waits
on it.

**2. ECHO — finish round 2 and merge** (`claude/echo-charlie-round-2`, 2 commits, PM-audited).
Nine of eleven audit items are already fixed and the rig is green (delta-clip 139 · prompts 108 ·
check-life 15 · behaved 39 · tsc clean). Two left, both in the box below.

**3. PM — audit the code cleanup, then merge it** (`claude/refactor-server-routes-zbi8kp`, 2 commits,
58 files, ~9,000 lines moved, splits `server.ts` into `src/routes/` and adds an 800-line gate).
**It merges HERE, in the middle, not last.** By this point round 2 is in, so the cleanup absorbs it
cleanly — and Echo then builds round 1 ONCE against the finished layout instead of building it now
and fighting the move later. Never audited by anyone.

**4. ECHO — build round 1** (`docs/specs/charlie-behavior/echo-build.md`) plus the three Admin
numbers, against the clean layout. Merge.

**5. ECHO — the Testing card last** (`echo-build.md` PART 4). It can only show what the engine
records, so it cannot come earlier.

**6. MAPPER — his own PM's call.** Phase 2's fixes are finished and awaiting that PM's audit
(`claude/mapping-engine-contract-wecvcy`, 55 commits). Do not hold anything for him, and he must not
merge during steps 2 to 3.

**ADDIE runs alongside the whole time.** She is in `public/app.html`, Echo is in the calling engine —
no collision. Her queue: the duplicate statuses, and the double-tap zoom regression
(`docs/tasks/admin-double-tap-zoom-regression.md`). **She must NOT touch the two dead number boxes on
the App page** — Echo already removed them on his branch.

## Stale branches to clear
`claude/check-admin-email-bug-ygrc9z` (Addie's work is already on staging; branch is an old copy) ·
`claude/check-pm-mapping-audit-j16fbb` and `claude/check-pm-review-dbuf2k` (27 hours old, look
superseded — confirm then delete).

## What the PM owes the owner next
- The code cleanup audit (step 3) — nobody has checked 9,000 moved lines.
- Round 2's last two items verified after Echo fixes them.
- A ruling the owner owes: the machine-phrase gate. Echo deviated from the spec DELIBERATELY and
  escalated it, and his reasoning is sound — today the engine cannot tell a person from a recording,
  so the gate the spec asked for would break on the exact case it exists for. He used "only the
  store's FIRST line counts" instead. It becomes the right gate once round 1 lands. **Owner to rule.**
- Two promotes waiting on the owner's word: the in-stock owner email still sends on the real site,
  and everything built since 07-30 is staging-only.

## The law for all of it
`docs/specs/charlie-behavior/README.md` is how Charlie must behave, in the owner's own words. If it
and the code disagree, the document is right and the code is the bug. `echo-build.md` and
`echo-build-round-2.md` are the work orders beside it.
