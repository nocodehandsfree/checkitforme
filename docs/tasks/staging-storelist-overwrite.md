# Staging store-list overwrite mystery

**What:** Quarantine wrote 105 bad numbers on prod; staging showed 33 — something re-imports staging retailers. Prod is correct; corner WHAT overwrites staging.
**Done when:** The overwriting job is identified and stopped; staging store list stays stable for a week.
**Lane:** DD
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
