# Missing CD comps for data-heavy Admin pages

**What:** The data-heavy Admin pages (Stores Intel/Search, Calls results, Users) have no comp on ADMIN_COMPS.dc.html. CD submits to docs/design/comps/inbox/ only.
**Done when:** Each data-heavy Admin page has a comp on the admin board; inbox files implemented, gap-checked, deleted.
**Lane:** CD + Addie
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
