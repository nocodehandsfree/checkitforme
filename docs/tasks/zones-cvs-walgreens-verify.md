# Zone run on CVS/Walgreens since the engine rebuild

**What:** Zones dial the same engine as single checks (2080731); Mapper's VAD patch reverted (8eb8d22). Needs a real zone run on CVS/Walgreens — real calls, owner judges the sound.
**Done when:** A zone run on CVS/Walgreens completes; owner says the calls sound right.
**Lane:** Echo (owner listens)
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
