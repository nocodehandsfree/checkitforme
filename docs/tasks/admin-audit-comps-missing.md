# No comp for Calc / Fun / Users / Search

**What:** Calc, Fun, Users and Search have no comp on ADMIN_COMPS.dc.html and don't cleanly fit the
five page types (LIVE/REPORT/LOG/CRUD/CONSOLE). Calc and Fun are internal sandboxes (see the cut
tasks); Users and Search are real operator surfaces that need a comp. Extends the existing
`admin-cd-comps-data-pages.md` (which already flags Intel/Search/Calls/Users). CD submits to
`docs/design/comps/inbox/` only.
**Done when:** Users and Search have an approved comp on the admin board; a decision is recorded for
Calc/Fun (comp them, or cut them per the cut tasks).
**Lane:** CD + Addie
**Tag:** comp
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
