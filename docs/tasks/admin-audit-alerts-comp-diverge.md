# Alerts diverges from its comp (built CRUD, comp says LOG)

**What:** The Alerts page is built as a CRUD list (each message a row) with the send-log folded into a
sub-sheet, but its assigned comp is the LOG grammar (1c). Reconcile: either update the comp to match the
CRUD reality (each customer message is genuinely an editable row) or rebuild to the LOG pattern.
**Done when:** Alerts matches an approved comp — the page-type and the comp agree.
**Lane:** Addie (+ CD if the comp changes)
**Tag:** comp
**Status:** open

**Verify-live output (paste on close — a task without it is NOT closed):**
```
(none yet)
```
