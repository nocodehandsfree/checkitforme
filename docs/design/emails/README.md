# design/emails/ — rendered email design mocks

**What this is · who it's for:** static HTML mockups of transactional/alert emails (the look), for
Design + Website. The alert email **build spec** lives in `docs/specs/alerts/`.

| File | What it is |
|---|---|
| `check-email-alerts-design.html` | Design mock of the stock-alert email (moved out of the repo root). |
