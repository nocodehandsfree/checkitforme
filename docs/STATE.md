# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-27 (Chains rebuilt; Admin's own calls now write themselves down).

## Admin calls write themselves down now, and mapping is ONE button (07-27)
- Every call you start from Admin used to vanish: no timeline, no seconds, no cost, nothing to open
  afterwards. Now you can open any mapping call and see the whole thing.
- **Say "promote" to switch that on for real.** Admin's buttons talk to the real site, and the real
  site does not have the recording code until you promote. It is recording on the test site today.
- Mapping is one button now. Re-map always runs the whole job, including finding the fastest way
  through the menu, and a sheet spells out exactly what will happen before you tap it.

## The Chains page is rebuilt, live on your Admin now (07-27)
- Tap a chain and the list gets out of the way. One screen: what a check costs there, how long to a
  person, what the menu costs, then the menu as a list of steps with the second each one fires at.
  The list reads "Bravo 41s" now, not the word "mapped". Checked on all 131 real chains: CVS 6.1¢, and
  you can SEE the 19 seconds of ringing after its menu ends. NOT checked on a real phone.
- Money bug fixed on the way: the agent rate behind every Calc number was 15% too high. The dashboard
  plan is written and waiting on real checks: `docs/specs/admin-ops-dashboard/CONTRACT.md`.

## The store map is real knowledge now (07-26)
- Every mapping call keeps its own proof: what we said, which recording we said it after, how fast we
  reached a person. Repeat calls raise trust; a route that CHANGES waits for your yes; nothing gets
  overwritten. Two things the old map hid: 16 chains were just the robot mashing 0, and 46 claim "a
  person answers" with no call behind it. Both flagged. Half the keypad chains stored a key with no
  timing, so checks pressed NOTHING (HomeGoods, Big 5, GameStop). Fixed.
- **Called a CVS in Anaheim at 11pm to prove it.** Desk in 62 seconds, 5 faster than we had on file. It
  also caught us counting "a person answered" from "transferring you now", about 17 seconds early.

## Every check writes a receipt — staging (07-26)
- Open any check and see the whole call with real seconds and money: dialed, ringing, every menu step,
  when a person answered, when the agent joined and left. Dead air is the number we drive down.

## Target: 3000 or higher = small store, press 3; below 3000 = desk on 2. 11 of 11 blind (07-24).
## Site fixes (five) — DONE on staging, waiting on you. Check your phone, then say "promote".
## Decisions waiting on YOU
- Say "promote" to push finished work to the real site (five site fixes + alerts and zone-lane fixes).
- Hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no)
## Next actions
- Site + Admin work their queues; Data chases the staging store-list overwrite. All: docs/tasks/INDEX.md.
