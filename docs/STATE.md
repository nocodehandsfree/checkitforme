# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-28 (the dashboard costs real checks; the engine waits on six calls).
## The dashboard tells you what a check costs, off real checks (07-28)
- Slide the top of the dashboard and the first thing it says is what a check costs you, with the last
  week beside it. Tap it: what happened, how we got into the store, where the seconds went, and how
  many tries it takes to get one real answer. Every number is added up from finished checks. Nothing
  is guessed, and old checks stay out because their costs were wrong.
- **It reads "no finished checks yet" right now, and that is TRUE.** Only Fun test-store calls have
  been priced, and test calls never count. Your first real check on the new engine fills it in on its
  own. **It also needs a production release before it shows on your phone at all.**
- Opening one check shows the whole call step by step with what it cost. It was printing a 5 cent
  call as 5,282,200 cents. Fixed, and a check too old to have been recorded now says so.
## The Chains page is rebuilt, and it tells you what needs YOU (07-27)
- Tap a chain and the list gets out of the way: what a check costs, how long to a person, then the
  menu step by step with its seconds. CVS 6.6¢, and you can SEE its 19 seconds of ringing. Every chain
  shows how much we trust its route; 33 are flagged. A better way through a menu appears above the old
  one: **Use it** or **Keep the old one**. NOT checked on a phone.
## The store map is real knowledge, and every check writes a receipt (07-26/28)
- Every mapping call keeps its proof; repeat calls raise trust, a changed route waits for your yes.
  A store answering differently gets its own route; a chain needs three stores agreeing.
  **They can hear now**, so nobody counts as answering unless they speak. **Read one:** a chain → **Mapping calls**.
## The new calling engine is finished and waiting on your six test calls (07-28, staging)
- The store hears the question the second they say hello, in our own voice, while the thinking part
  warms up behind it. It can't talk over their answer, and if Staff walk away we stop paying.
- **Turn it on before you test: Admin → Global → How calls are made → New calling engine.** It reads
  OFF, and with it off a test call takes the old path. We also stopped re-recording lines, ~7¢ each.
- **Stopped a real problem:** a store mapped as a menu that now answers directly was firing keypad
  beeps into a live person's ear. We stop the moment somebody answers.
- **Waiting on YOU:** the six test calls, and whether we pay to upgrade our voice. The phone company
  won't let us use our own cheaper thinking with a copied voice, over half of every call.
- **Fixed 07-28 from your test calls:** no longer rushes your greeting, no longer hears its own voice
  as Staff, Stop writes "cancelled", it asks ONE question and wraps, the conversation shows up on the
  finished check, and the Live/Staging switch finally steers the Designer both ways.
- Target: 3000+ = small store, press 3; below 3000 = desk on 2. 11 of 11 blind (07-24).
## PROMOTED 07-27 — the five site fixes, the rebuilt plans and checkout, the call recording and the
Use it / Keep the old one buttons are live. **Nobody has seen any of it: the real site still shows the
coming-soon splash.** Walk it on your phone before you lift that.
## Waiting on YOU: hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no)
Next: Site + Admin queues; Data chases the staging store list. Everything: docs/tasks/INDEX.md.
