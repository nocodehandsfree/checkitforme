# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-28 (the new engine is on for staging and waiting on your six calls).
## The dashboard tells you what a check costs, off real checks (07-28)
- Slide the top of the dashboard and the first thing it says is what a check costs you, with the last
  week beside it. Tap it: what happened, how we got into the store, where the seconds went, and how
  many tries it takes to get one real answer. Every number is added up from finished checks.
- **It reads "no finished checks yet" right now, and that is TRUE:** only test-store calls have been
  priced, and those never count. Your first real check fills it in. **It also needs a production
  release before it shows on your phone at all.**
- Opening one check shows the whole call step by step with what it cost. A check too old to have been
  recorded now says so.
## The Chains page is rebuilt, and it tells you what needs YOU (07-27)
- Tap a chain: what a check costs, how long to a person, then the menu step by step with its seconds.
  CVS 6.6¢, and you can SEE its 19 seconds of ringing. Every chain shows how much we trust its route;
  33 are flagged. A better way through a menu appears above the old one: **Use it** or **Keep the old
  one**. NOT checked on a phone.
## The store map is real knowledge, and every check writes a receipt (07-26/28)
- Every mapping call keeps its proof; repeat calls raise trust, a changed route waits for your yes. A
  store answering differently gets its own route; a chain needs three agreeing. **They can hear now**,
  so nobody counts as answering unless they speak. **Read one:** a chain → **Mapping calls**.
## The new calling engine is ON for staging. Your six test calls are the last thing (07-28)
- **Nothing to flip. It is already on for staging**, so a check from staging.checkitforme.com goes
  through the new engine now. I ran one: the finished check shows the call second by second and what
  it cost, 1.7¢. The Fun store's line hung up five seconds in, so nobody talked. That part is yours.
- The store hears the question the second they say hello, in our own voice, while the thinking part
  warms up behind it. It can't talk over their answer, and if Staff walk away we stop paying.
- **Two switches are gone, because neither was ever a choice.** We stop the keypad the moment somebody
  answers, and we hang up the thinking whenever we're put on hold. Both on every call now.
- **Listen for three things:** it asks once and waits · after a hold it carries on instead of greeting
  you again · it never beeps the keypad once you are talking.
- **The Live/Staging switch now steers the call settings too**, so the screen you read and the site
  you change are the same one. Everything else there still edits the live site, as before.
- **Waiting on YOU:** the six calls, and whether we pay to upgrade our voice. The phone company won't
  let us use our own cheaper thinking with a copied voice, over half of every call.
## PROMOTED 07-27 — the five site fixes, the rebuilt plans and checkout, the call recording and the
Use it / Keep the old one buttons are live. **Nobody has seen any of it: the real site still shows
the coming-soon splash.** Walk it on your phone before you lift that.
## Waiting on YOU: hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no)
New 07-29 on a PR: three agent guard rails (look first · no new Admin pages · recite the task first). Next: Site + Admin queues; everything: docs/tasks/INDEX.md.
