# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old, history is in
git. Last updated: 2026-08-06 (the hold fault he found is fixed and on staging; every customer can now call off a check).
## THE ROBOT STORE COULD NOT FINISH A CHECK, AND NOW EVERY SCENE IS WRITTEN (08-06, PM). Nine of your seventeen tests had no script at all, and six of the eight that did stopped one turn too early: Staff gave the answer, Charlie asked his follow up, and the robot had nothing left to say, so the check died with no goodbye. That is why the walk kept failing on things that were not Charlie's fault.
- **Every one is written down now** in `docs/specs/robot-store/scenes-needed.md`: all seventeen against the ten scenes, what fails and why, and your approved word for word script for each missing one. Plus two you added today, test 18 (a check for one exact product, which arrives with hobby stores) and test 19 (hold music with an advertising voice in it, the most dangerous hold because a recorded voice is the closest thing to Staff coming back that is not Staff).
- **The sound clips are picked.** Both store recordings, since the two volumes are themselves the test, and two hold music tracks from our own phone company, one of which starts quiet and swells. Sources and trim points are in the same file. Echo sets the volumes against the Staff voice and writes the numbers down, because a recording played at whatever volume it happens to be proves nothing.
- **Your move: none.** Echo has the first prompt (the six broken scenes plus everything that needs no audio) and the hold set follows.
## THE CHECK STATUS SCREEN NEVER CHANGES ITS ANSWER NOW (08-06, ON STAGING). When a check ended, three different parts of the page each went and got the answer on their own clock and each painted what it got back, so one answer replaced another on your screen: "Check cancelled" for a second, then "Couldn't tell". The first one there now does the waiting and the others keep quiet, so the screen holds on "Getting the answer" and prints the settled answer once. Stopping a check yourself still cancels it and is still never charged, and that half is real money: I proved on staging that simply READING a cancelled check's result flips its own record from "Check cancelled" to "In stock", which is the state that bills. **Your move: run a check on your phone, end it however you like, and tell me if the screen ever changes its mind.**
## The site chat closed with a handoff (08-06). Next agent's first read: `docs/team/site/checkpoint.md`, then `docs/tasks/site-auto-checks.md`. Auto-checks is the next build and its pictures are NOT approved: only the list screen is. Your ruling on the report is written down in `docs/specs/auto-checks/README.md` in your own words, along with all five pictures and the scripts that made them. The bug list that is still open is `docs/tasks/go-live-site-audit.md`, and the biggest one is that production has never sent a single email because nobody has confirmed an email address there. **Your move: sharpen the auto-check pictures with the next chat.**
## The Activity report is honest now (08-06, ON STAGING, driven live). One press on a bar picks that day and stays there. Pressing the same bar again used to clear the day and quietly swap the four counts from that one day to the whole week, which is where your 22 in stock came from, and the green bar vanished with it. The checks listed under the counts are that day's own now, up to six, instead of the same newest checks showing on every day. Store logos there were broken too and the logo chat fixed that separately. **Your move: open My checks, Activity, and tap a few days.**
## THE TESTING SHEET IS DONE AND THE 12:54 AM CHECK IS THE CLEAN RECORD (08-06, staging + Admin, every scorecard row green, 5.9 cents). Order end to end: test name, dialing, ringing, line answered, Staff greeting with the greeting, Delta with its question, Charlie joined and warmed, Staff answered "Yeah.", the set question, Staff's Pitch Black answer, the goodbye shown open, check ended, then the double check, In stock, Customer charged, always last.
- **From your screenshots, all fixed:** markers now sit on their text line and the rail no longer bends at In stock; the note names WHICH answer ("Charlie understood the stock answer and was told to say goodbye when done"); the Testing list already labels checks by their time and that is how they get talked about now.
- **The double check reader, measured not guessed:** Gemini's paid price, Groq's small model and Groq's big model all scored 6 of 6 on the robot store's own conversations, trick cases included, so the smallest won: Groq at 0.006 cents a read, cheaper than Gemini. The big Groq model from earlier cost MORE than Gemini paid, which defeated the point.
- **Issues print where they happened:** any behavior row that fails now lands in the check log, red, at its second, with the rule that commands the behavior quoted under it. Built before the test walk, proven by faking a failure and watching it land in place.
- **HANDOFF DONE (08-06): the In stock circle sits on the rail now, proven by measuring the rendered sheet in a real browser, all markers on one center.** The owner starts a FRESH chat for the test walk, one robot scene at a time, beginning with scene 2, the clear no. The list of ten lives in the voice-calls checkpoint with the scene to card map and the landmines.
## The support chat is being tested like the robot store tests checks (08-05/06). A robot customer runs real conversations against the staging chat and every reply is scored out of 10: `docs/specs/support-chatbot-testing/SCORECARD.md`. Round 1 scored 250 out of 270. Fixed and live: asking for a person used to answer "tap Help in the footer", which reopens that same chat, so nobody could reach a human; the chat said an endless hold is free when your ruling charges it, fixed in the chat AND in six pages of the book on your go; the Spanish result screen said "Sin cargo" on three statuses we charge. Round 2 signs in and uses the 291 checks already on your account, placing none, and found FIVE money bugs, all fixed: a perfect check that answered in 24 seconds was refundable on request, a hold was refunded so we charged and refunded in one breath, and twice the chat answered about a different check than the one you opened it from. **Your move: GameStop. You say it should not be callable and the store list serves it as callable, so the chat says yes to it.**
## The reply lock rebuilt as the renderer (owner's go, 08-05, both AI reviews agreed). An agent writes its best full answer; a dedicated writer (Sonnet) sees the owner's saved message plus a few real approved examples and puts the answer in his voice; every number, path, command, and quote is machine-checked against the original, then a meaning pass catches anything added, dropped, softened, or changed. Approved text sends and the chat closes right away. Short replies (2 lines) skip everything. A broken renderer fails open, never mutes, never hangs. Examples live in `.claude/reply-examples/` and grow from the owner's pasted complaints paired with fixes he approves. Honest limit, stated to the owner: more natural and consistent, not a guarantee every reply sounds human; a true guarantee needs a separate app. Older chats: send "pull staging" once. **Owner's move: none.**
## GitHub's mark on staging is GREEN again (08-05, verified job by job on commit 3a0dd587: tests, secret scan, doc sizes all passed; the whole test run finished in 3 minutes where it used to hang 25). Two real causes, both fixed: the secret scan's 29 hits are all old history or third party PUBLIC keys in a docs snapshot, now fingerprinted in `.gitleaksignore` (no secret values; a planted token still fails the scan, proven). And the test run hung because three suites shared port 8798 while one waited on a curl with no time limit: qa-pages moved to 8790, its waits capped, EVERY suite now bounded at 420s and named if it stalls, and `kill-tests.sh` no longer shoots its own caller. **Owner's move: none.**
## Store logos: all 112 re-cut, one place decides their size (07-31/08-01, staging). New pictures are LIVE on the real site and Admin; the code half needs a promote. **Change a logo in one spot and it now really does change everywhere** — the logo wall is the record of truth and cannot lie to you.
## Shipped since 07-30, all on staging unless noted: the check status page follows its newest line, both screens clear Safari's bottom bar, and the wait on ElevenLabs is GONE (at hang up we settle from our own words plus our finished read). In stock alerts: every finish path sends, alerts stay on until paused, one store = ONE email; 08-02 in Admin (LIVE) the owner email Off switch saves, the Statuses page works, and every Yes button was answering No — Off on the real site needs a promote. A wrong department no longer costs you the check: ask once to be put through, meter off through the hand-over, ask again when somebody new picks up. The bottom bar can never go dark again and Live is cleaned: the tint is nailed down, three ways it used to break are refused before anything goes live, and cost per check opens on The baseline, your 6.7¢ ceiling off your real prices. **Your move: tap the Fun store alert once, run a check, one email.**
## The new calling engine is ON for staging. Your six test checks are the last thing (07-28/29)
## Mapping, parts 1 and 2 of 3 — BUILT, waiting on your phone (08-02)
- Mapping talks to machines, Charlie talks to people: the check that proves the department hands the SAME live call to Charlie, the right department is his word, and a mapping check never rides a transfer to get there. A chain whose menu changed now GLOWS red on the chains page instead of wearing a small mark, the "Menu changed" choice in the dropdown picks exactly those, and the Review row is gone: nothing on that page asks you to look at anything.
- A store whose menu changed takes itself off the website, files ONE job however many times it fails, gets re-mapped on its own with nobody pressing anything, and puts itself back when that re-map succeeds — one line of history, and its own page reads as not found while it is off. Three stores of one chain off means the CHAIN's menu changed: never mute the chain, set its fast way aside so checks use the careful full words. One store can also be muted by hand in the stores section, and it never travels between staging and the live site. **Chunk 3, the screens, started (08-03, on staging + Admin):** a chain whose store took itself off the website now shows the red alert icon and appears under "Menu changed" in the filter, and both clear themselves when the store heals. No check says "Recipe winner" any more — the recipe box and Menu say what we run. On Menu, a choice proven wrong is struck through in red. Driven on staging: muted CVS Mulholland, CVS flagged, its own page read as not found, unmuted, the flag cleared. **Also in:** the first check of a run is headed "Proving department" (it is the one that asks Staff), three boxes at the top of the chains page you can tap to filter, the filter row on one line, and fixing a word on the Menu asks first. **To see it with data:** a staging-only button fills a chain with one full mapping run — a proving check, a listening check, a failed speed try with its reason, and a speed win. **Third audit round done (08-04, on staging + Admin):** "Speed optimized" only turns green on the single fastest check now, a store that answers differently reads "Not used" instead of waiting on you, and a choice we marked wrong stops blocking us once the store is re-mapped — nothing is deleted, the old marks stay on the screen against the map they were learned on. I drove the chain page at phone width and everything fits; 734 checks across nine sets, none red.
## PROMOTED 07-30 (owner's go, PM drove it) — everything built since the 27th is on the real site and Admin: the chain page's server half, the Testing scorecard's server half, the wrong-department save, Spanish names on the plans. The new engine stays OFF on the real site until you flip it; customers still see the coming-soon splash.
## A store that answers itself, and it found real problems on day one (08-02, working on staging)
- It picks up on 424 484 7395 and plays a real person's exact words, ten different ways. All ten ran for real today from the staging site, tapped like a customer. Your MVP store points at it; Fun unchanged. Every greeting landed first and on its own line, live and written; plain yes and plain no come through clean.
- **The one that used to cost us money is a coin flip:** "We did, but it's not out yet" read as IN STOCK on one check and "couldn't tell" on the next, same words, minutes apart.
- **The money one: once Staff put us on hold, nothing they say afterwards is written down.** One said "yeah, we've got a few" after checking and we filed it as left on hold; another came back with "I did not see any" and the screen said IN STOCK while the record said nobody answered. Same shape on a transfer and on a hang-up. **Your move: nothing — it is the next voice job, with a test that proves the day it is fixed.**
## Charlie is never switched on to talk to nobody again (08-02, ON STAGING)
- The setting that opened him after a set number of seconds with no voice heard is DELETED — it switched him ON to an empty line and billed for it, the line on your own check log at 63 seconds. A second box wired to nothing went with it.
- Your check log can now say **who really hung up** (including our own time limit, so we never blame a store for our own accounting), a misheard machine phrase can no longer end a check on a real person, and two things the log claimed but never detected now work: being sent back through the phone menu, and a check dying during a transfer. **Not phone-proven — that is the robot store's next run.**
## The Testing screen you approved is LIVE and the engine behind it is built (08-04, staging + Admin)
- **Tap any check in Voice ▸ Testing and you get the exact screen you approved**: the test's name and what it proves up top, passed and failed pills, the four tiles (total cost · total time · Charlie talked with your color rule · profit against your 67% floor), your five cost buckets with real rates behind every tap, the behavior rows, and the whole check end to end with the spoken lines inside it. Your old checks render too.
- **Charlie finishes a check now. Both bugs fixed, PM audit PASSED (08-04 late):** the goodbye order was being delivered to a name no check held, so it reached nobody on every real check while every test stayed green; and after his goodbye the quiet read as Staff stepping away, so the line sat open until the store hung up on us. Now the order arrives, and the quiet after his goodbye is us putting the phone down. He also stopped asking twice. PM's own fresh check: goodbye spoken, ended 9 seconds after the answer, right status, 6.5 cents.
- The robot store also caught and Echo fixed: one loud click on the line was convincing us everything after it was background noise, so Staff coming back were never heard.
## THE HOLD FAULT HE FOUND IS FIXED, AND EVERY CUSTOMER CAN NOW CALL OFF A CHECK (08-06, ON STAGING). **His check 298: he told Charlie to hold, went quiet, then asked "hello? are you there?" several different ways, and Charlie never came back and none of it was written down.** Two reasons, both replayed on the rig before and after the fix. Being back needed most of a second of talking WITHOUT A PAUSE IN IT, and somebody checking whether we are still there says short things with long pauses, so it never counted once; it is counted over the last three seconds now. And the ear measures whoever speaks against how loud that person was earlier, and that memory never faded during a wait, so anybody who came back quieter than a third of how loud they had been was written off as noise from across the room and could never be heard again. **A robot store test now comes back exactly the way he did, so this can never come back unnoticed.** **THE CANCEL: the Stop and hang up button existed only on your own account. Every customer gets it now, and it disappears the moment Staff pick up, which is where you said the cancel window closes. Driven: check 306 was dialed, rang, and cancelled at 2 seconds, and the screen read your own new wording, Check cancelled, You stopped this check from happening. No check. No Charge. Nothing was charged.** **Your move: run a check on the Fun store, tell Charlie to hold, and say hello a few times.**
## THE HOLD WORKS, AND THE CANCEL BUTTON IS MISSING (08-06, both read off real checks). **Charlie coming back from a hold is proven live: check 301 on the robot store, Staff walked away at 28 seconds, silence for 44, Charlie reconnected as part 2 and the answer landed as not in stock, the right answer for that test.** Your own check 298 never had a reconnect to fail: you stopped speaking and never spoke again, so nobody came back and Charlie correctly stayed off the meter. Check 300 was the same test stopped 20 seconds before the robot came back. **THREE REAL FAULTS 301 DID SHOW: the first words after a hold are lost ("Okay, thank you" never got written down) · after a clear no Charlie asked another question instead of saying goodbye, then the line sat quiet for 73 more seconds until the store hung up, 149 seconds and 11.9 cents against a 6.6 cent target · THE SCREEN SAID IN STOCK WHILE THE RECORD SAID NOT IN STOCK.** **CANCEL: a customer checking ONE store has NO way to cancel — the Stop and hang up button only shows on your own comp account. The status wording is "Check cancelled / You stopped this check from happening." and never says no check, no charge, though the check is genuinely never charged. Your move: rule the words and say whether every customer gets the button.**
## Repo cleanup DONE (08-04, staging): 32 unused files deleted, every doc pointer to a moved or deleted file repaired, and the browser tests (the files that clicked through the site like a customer) archived on your ruling. Late 08-04 the owner set the promote rule: promote now REFUSES unless GitHub's automatic test run is green on the exact staging code, and driving your change on staging stays required. First run of the new gate caught staging RED: the newest save (the reply lock audit bundle) fails the tests and the secret scan — that chat's push, that chat's fix.
## Waiting on YOU: promote now to stop the owner email on the real site? · hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no) · Next: docs/tasks/INDEX.md.
