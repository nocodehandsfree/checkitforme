# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-30 (the engine waits on six calls; the store-picker seconds line waits on mapping).
## The Policy screen sets your prices the way every other switch works (07-29, live on Admin now)
- Open Policy and the prices sit in the same rows as the on/off switches. **There is no Save button any more.**
  Type a number, tap away, it is saved and customers see it. Type a word and it bounces back. Every price stayed
  where it was. Your analytics ID moved to App, and two blocks you never read stopped loading, so it opens faster.
## The dashboard tells you what a check costs, off real checks (07-28)
- Slide the top of the dashboard: what a check costs you, with the last week beside it. Tap it for what happened,
  how we got into the store, where the seconds went, and how many tries it takes to get one real answer. Opening
  one check shows the whole call step by step. **It reads "no finished checks yet" right now and that is TRUE:**
  only test-store calls have been priced and those never count. Your first real check fills it in, after a release.
## The Chains page tells you what needs YOU, and the store map is real knowledge (07-26/27/28)
- Tap a chain: what a check costs, how long to a person, then the menu step by step with its seconds. CVS
  6.6¢, and you can SEE its 19 seconds of ringing. Every chain shows how much we trust its route; 33 are
  flagged. A better route appears above the old one: **Use it** or **Keep the old one**.
- Every mapping call keeps its proof; repeat calls raise trust, a changed route waits for your yes. A store
  answering differently gets its own route, a chain needs three agreeing, and **they can hear now** so nobody
  counts as answering unless they speak. **Read one:** a chain → **Mapping calls**. Not checked on a real iPhone.
## The new calling engine is ON for staging. Your six test calls are the last thing (07-28)
- **Nothing to flip. It is already on for staging**, so a check from staging.checkitforme.com goes through
  the new engine now. I ran one today, start to finish: it asked once, got a real answer, 5.3¢.
- The store hears the question the second they say hello, in our own voice, and if Staff walk away we stop
  paying. **Listen for three things:** it asks once and waits · after a hold it carries on instead of greeting
  you again · it never beeps the keypad once you are talking. The Live/Staging switch steers these settings too.
- **07-29, the engine's notes about itself are honest now.** The check you sent me claimed the store handed us
  on ten times and put us on hold ten times. None of it happened: one hiccup of sound got read as a phone ringing,
  over and over, and it said the thinking part joined three times when it joined once. Both fixed, today's real
  check proves it, and **two numbers you can tune yourself** sit on the calls settings page. Separately, ordinary
  checks were supposed to teach us each store's phone menu and never did. Fixed: every check improves the map now.
- **Waiting on YOU:** the six calls, and whether we pay to upgrade our voice. The phone company won't let
  us use our own cheaper thinking with a copied voice, over half of every call.
## PROMOTED 07-27 — the five site fixes, the rebuilt plans and checkout, the call recording and the Use it /
Keep the old one buttons are live. **Nobody has seen any of it: the real site still shows the coming-soon
splash.** Walk it on your phone before you lift that.
## The "seconds to reach the store" line is not true yet, and it waits on mapping (07-30)
- Every number on it came from the old data, not a mapping call, and 18 of the 42 are routes we ourselves
  flagged as fake. It also counts the department's phone ringing, which we will not promise. Written up and
  HELD until mapping proves real routes. Agreed words: "Hang tight. ~65 sec of their automated system."
## Waiting on YOU: hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no) · Next: docs/tasks/INDEX.md.
