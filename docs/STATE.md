# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-30 (the engine waits on six calls; the store-picker seconds line waits on mapping).
## The Admin can no longer un-clean itself (07-30, live)
- The cleanup rules are now a machine check that runs before any Admin change goes live. Once a screen is cleaned it
  is locked: put back a hover-only hint, a "click here" line, or a line you called untrue and the change is refused.
  It lists **18 problems across 9 screens** today; none locked yet, so nothing is blocked.
- **The cleanup itself waits on the new engine's test scorecard and nobody is building that** — no work exists for it anywhere, only the note. Say the word and I build it first.
## The Policy screen sets your prices the way every other switch works (07-29, live on Admin now)
- Prices sit in the same rows as the on/off switches. **No Save button any more:** type a number, tap away, it is
  saved and customers see it. A word bounces back. Every price stayed put; your analytics ID moved to App.
## The dashboard tells you what a check costs, off real checks (07-28)
- Slide the top of the dashboard: what a check costs you, with the last week beside it. Tap it for what happened,
  how we got into the store, where the seconds went, and how many tries it takes to get one real answer; opening a
  check shows the whole call step by step. **It reads "no finished checks yet" and that is TRUE** — only test-store
  calls have been priced and those never count. Your first real check fills it in, after a release.
## The Chains page tells you what needs YOU, and the store map is real knowledge (07-26/27/28)
- Tap a chain: what a check costs, how long to a person, then the menu step by step with its seconds. CVS 6.6¢,
  and you can SEE its 19 seconds of ringing. Every chain shows how much we trust its route; 33 are flagged, and a
  better route appears above the old one: **Use it** or **Keep the old one**. A changed route waits for your yes.
- Every mapping call keeps its proof and repeat calls raise trust; a store that answers differently gets its own
  route, a chain needs three agreeing. **Read one:** a chain → **Mapping calls**. Not checked on a real iPhone.
## The new calling engine is ON for staging. Your six test calls are the last thing (07-28)
- **Nothing to flip. It is already on for staging**, so a check from staging.checkitforme.com goes through
  the new engine now. I ran one today, start to finish: it asked once, got a real answer, 5.3¢.
- The store hears the question the second they say hello, in our own voice, and if Staff walk away we stop paying.
  **Listen for three things:** it asks once and waits · after a hold it carries on instead of greeting you again ·
  it never beeps the keypad once you are talking.
- **07-29, the engine's notes about itself are honest now.** The check you sent claimed ten hand-offs and ten
  holds; none happened. One hiccup of sound got read as a phone ringing, over and over. Fixed, today's real check
  proves it, and **two numbers you can tune** sit on the calls settings page. Ordinary checks also never taught us
  each store's phone menu, which they were supposed to. Fixed too: every check improves the map now.
- **Waiting on YOU:** the six calls, and whether we pay to upgrade our voice. The phone company won't let
  us use our own cheaper thinking with a copied voice, over half of every call.
## PROMOTED 07-27 — the five site fixes, the rebuilt plans and checkout, the call recording and the Use it / Keep
the old one buttons are live. **Nobody has seen any of it: the real site still shows the coming-soon splash.**
## The "seconds to reach the store" line is not true yet, and it waits on mapping (07-30)
- Every number came from the old data, not a mapping call, and 18 of the 42 are routes we flagged as fake. It also
  counts the department's phone ringing, which we will not promise. HELD until mapping proves real routes.
## Waiting on YOU: hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no) · Next: docs/tasks/INDEX.md.
