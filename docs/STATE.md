# STATE — the owner's single source of truth
One screen. Every session updates this at close (≤40 lines). New truth REPLACES old — history is in
git. Last updated: 2026-07-28 (the store hears the question the moment they pick up).

## The Chains page is rebuilt, and it tells you what needs YOU (07-27)
- Tap a chain and the list gets out of the way. One screen: what a check costs, how long to a person,
  what the menu costs, then the menu step by step with its seconds. CVS 6.6¢, and you can SEE the 19
  seconds of ringing after its menu ends. NOT checked on a phone.
- Every chain shows how much we trust its route: proved, seen once, or needs a look. 33 are flagged.
  A better way through a menu appears as a card above the old one with the calls that proved it:
  **Use it** or **Keep the old one**. Mapping is one button: Re-map runs the whole job.
- ⚠️ The agent rate behind every price was reading 15% CHEAP. A real Target check is 5.2¢, not 4.7¢.
  Fixed, and the page now reads the calling engine's own rate so the two cannot drift apart again.

## The store map is real knowledge, and every check writes a receipt (07-26/27)
- Every mapping call keeps its proof; repeat calls raise trust, a changed route waits for your yes,
  nothing is overwritten. A single store that answers differently gets its own route, live by itself
  ONLY where the normal route already failed there. A whole chain needs three stores agreeing.
- Open any check and see the whole call with real seconds and money: every menu step, when a person
  answered, when the agent joined and left. Dead air is the number we drive down.
## The store now hears the question the second they say hello (07-28, staging)
- Before: the person picks up, then we start waking the expensive part up, and it opens by talking.
  Now a recorded line in our own voice asks the question immediately while the thinking part warms up
  behind it. It hears nothing until the question is finished, so it can't talk over their answer, and
  anything they say early is held and handed over whole. Three separate ways to know the line finished,
  and if all three miss it takes over anyway rather than leave someone talking to silence.
- We were re-recording the same lines on every single call, about 7¢ each. Recorded once now.
- **Stopped a real problem:** a store we mapped as a menu that now answers directly was getting keypad
  beeps fired into a live person's ear. We stop the moment somebody answers.
- **NOT verified on a real call** — the test store rings YOUR phone, so I proved it against a fake
  phone line end to end instead. It needs one real call to a store to be believed.
## Target: 3000 or higher = small store, press 3; below 3000 = desk on 2. 11 of 11 blind (07-24).
## PROMOTED 07-27 — everything is on the real site now
- The five site fixes, the rebuilt plans and checkout, the call recording and the Use it / Keep the old
  one buttons are live on checkitforme.com. **Nobody has seen any of it — the real site still shows the
  coming-soon splash.** Worth walking it on your phone before you lift that.
- Admin is PARKED waiting on Echo and Mapper. On your go: the Search store-call button, then the dashboard.
## Waiting on YOU: hide the fake (simulated) poll rows from the Admin feedback queue? (yes / no)
## Next actions
- Site + Admin work their queues; Data chases the staging store-list overwrite. All: docs/tasks/INDEX.md.
