# NEXT BOX for the rehearsal chat (Echo 2) — HELD until the owner's go (PM, 08-18 night)
Owner's order of events first: he snapshots usage, the simulation agent runs 100 simulation runs
for the cost baseline, THEN this box goes to the rehearsal chat. Do not start early.

```
Task: teach Echo to recognize hold music by its
sound, not only by the missing pauses. One test
at a time, robot store only, never the Fun store.
Never push code while a call is running.

Done means all of these, proven on the record:
1. On a music hold, Echo marks music within about
   1 second of it starting. Show the stamp.
2. The drop rule goes back to one number: 3
   seconds everywhere, music same as quiet.
3. Re-dial the growing music test until it passes
   with the drop row green at 3.
4. Re-dial the loud classic music test. Report its
   new meter floor. Goal: under 31 seconds.
5. The whole replay bank still passes. No change
   to how a talking person is treated.
6. If any step would trade safety for speed and
   risk missing a Staff answer, stop and write it
   up instead of building it.
7. Build the advert judging: after a call ends,
   the model that re-reads the record decides if
   the hold announcement was a person or a
   recording. Prove it on the advert scene's
   saved recordings before any new dial. If it
   keeps judging wrong, reword and re-run on the
   same recordings; escalate to the PM, never
   stop a live call over it.

Reuse what exists: the code that listens on
holds, the robot store's practice scenes, the
saved replays of past calls, and the grading.
Nothing new built beside a working piece.

Order of work: build the music hearing, prove it
on replays, then dial one test at a time to done
before touching the next. Report every check by
its number so the PM can audit each claim.
```

Also his, outside this box: hold give-up clock to 100 seconds (Admin > Settings > Hold cap
seconds, still 120, not set); the test's NAME replaces the status word on test-check rows, icon
carries the status (simulations side); his final judging dials on tests five, six, seven.
