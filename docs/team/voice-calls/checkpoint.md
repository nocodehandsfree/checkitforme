# VOICE-CALLS — checkpoint (current state)
> Calling engine + voice tuning + phone-tree mapping. `src/voice/` is FROZEN (machine-locked; owner
> task + repo-root `.unlock`). Charter: handoff.md + MAPPING-MANUAL.md. <=60 lines. **LAW: Admin is the record of truth.**
## THE LIVE CALL RUNTIME — all of `docs/specs/live-call-runtime/` built, ON for staging. READ THAT SPEC FIRST; this file is only traps + what is open. §3's six calls need HIS phone (`scripts/gate-zero.ts`).
- **Delta is ONE CLIP**, Charlie prewarms behind it. **`prewarmTimer` MUST stay OUT of `clipTimers`** or a short clip leaves NO agent. **NO VOICE = NO CHECK.**
- **HOLD: `reopen` ALWAYS runs**. **THE FLAP IS DEAD** (receipt 199: 10 false transfers on a direct dial). ONE 20ms frame declared a transfer; BOTH edges need a RUN — `transferToneMs` 600 (a real burst is 2000) · `backVoiceMs` 400, in `tuning.ts`. **A transfer ALSO opens `hold_start`**; **`charlie_join` fires ONCE**. **07-31 TIMER AUDIT: NOTHING ends a mid-check hold** — only `maxCallSeconds` (300 on staging, ridden to Twilio as `timeLimitSec`). `holdMaxSeconds` (Admin "Hold max seconds", 60; code fallback 45) is **NOT a hang-up** — it OPENS Charlie when no human was ever heard. `ivrMaxSeconds` (90) is **WIRED TO NOTHING** — a dead Admin box. `ringMaxSeconds` 35 is the ONLY real give-up. **DECIDED 08-01: hold cap 120s, and `holdMaxSeconds`/`ivrMaxSeconds` are DELETED — see the Charlie record.**
- **§7 BRAIN — BLOCKED BY *THEM*, PROVEN 07-31, NOT OUR CONFIG.** custom LLM + an INSTANT clone is refused on agent CREATE **and** UPDATE (`custom_llm_not_allowed_in_with_agent_with_ivc_voice`); the SAME setting is accepted instantly with a `premade` OR `professional` voice — so it is the VOICE, nothing else. Branson HD is `cloned`, `is_allowed_to_fine_tune:false`. Plain `/v1/text-to-speech` with it returns audio (200), so building the talking ourselves keeps the voice. Creator plan: `professional_voice_limit:1`, `voice_limit:30`. **Owner emailed them to lift it**; else PVC (30 min + the real speaker verifying) or we own the conversation. Only route NOT behind the admin login; `..._OURBRAIN_AGENT_ID` unset.
- `call_dropped` is **never written `completed`** (the only status the one-hour block matches); Charlie bills as the SUM of open stretches. **`rollupFromRow` is the ONLY reader of a stamped call row**, both receipt routes use it.
- **ONE QUESTION, BOTH LANES.** `declaresOneTurn` (tapedeck): an EMPTY `followups.type` = set+format folded into the `set` line. Branson Global's lines say **WORD FOR WORD** or the agent rewrites them and loses the day.
## 07-29 THE READER RULE WAS HALF-WIRED. Three of five finalize paths passed `needSecond ? second : null`, false exactly when the live read had an opinion, so live=IN + reader=OUT became a green AND a charge. **`consensusFor` reconciles ALWAYS**; **spec gate 4 FAILS THE PUSH** otherwise. `Test — One Question` (Branson HD, Fun store) = THE WORKFLOW HIS SIX CALLS RUN; `scripts/make-test-workflow.ts` is the definition AND the writer. **`vt_*` is staging-only.**
## SWITCHES + WHAT WE MADE IN THE ACCOUNTS. Do not delete these wondering what they are. `cheapBridgeAll` **THE BIG ONE: OFF = every check takes the old path. ON for STAGING**, prod OFF till a promote · `ourBrain` blocked §7 (**Charlie on Anthropic API**) · `askForTransfer` ON. **`stopKeysOnHuman` + `closeAgentOnHold` ARE NO LONGER SWITCHES**: forced true in `getPolicy` AFTER the merge, because both saved blobs carry false. **Every guessed number is in `src/calls/tuning.ts`.** Agent `agent_2301kyk2rwgyfg8r50xk9enqwy2r` = staging `ELEVENLABS_MIDCALL_AGENT_ID`, **PROD NOT SET**; `check_brain_key` ↔ `BRAIN_API_KEY` (staging only). **`ENV_FLAGS` (app.html) must equal `KEEP_LOCAL_FLAGS` (settings-sync)** or the prod mirror stomps a staging test inside a minute; a spec check compares the lists. **A NEW call flag goes in BOTH.**
## THE WRONG-DEPARTMENT SAVE: SHIPPED 07-30, `askForTransfer` DEFAULT ON, Calls ▸ App (comp 1i), rule in `prompts.ts` on `{{ask_for_transfer}}`, BOTH lanes on the ONE switch. **WORDS, never the Ear (§10)**: `heardWrongDepartment` → `unknown`+`wrongDepartment:true` → `learnFromReceipt` + decay. **A hand-over is ALWAYS a new person** · the gap note must survive a reopen · a SILENT hand-over is caught because he ASKED (`askedToBePutThrough`, ONE copy, bridge AND scorecard). Gates **O.5 · O.6 · O.7**. `heardWrongDepartment`: the hand-over word is REQUIRED (bare `put you` matched "put you on hold"); a bare counter greeting ("Pharmacy, this is Joe") counts; `handingOver` is worked out ONCE and rides EVERY reason, and STAFF moving us marks the next wait a hand-over without us asking.
## 08-01 HIS TEST DAY (runs 222-226, ledger: `test-day-2026-08-01.md`). Five runs, seven faults, all fixed; `test-delta-clip.ts` replays every one. Billing RULED: a hold drop IS charged, biller+refunds already right, the page's "No charge" string is a boxed WEBBIE task (EN+ES).
## 07-30 TESTING = CHARLIE BEHAVIOR, **THREE rows, ONLY Charlie**: Meter stopped · Transfer requested · Re-asked after transfer. DELETED, do NOT restore: `asked_once` · `mapping_held` · `no_keypad_at_person` — **OWNER RULE: walking a menu is not a test; the check failing IS the report.** A row belongs only if a WORKING check could hide it from him. Copy ASSERTED in `test-behaved.ts`. **`callSeconds` is OLD-path only → `rollupFromRow` reads the last step.** **PM: promote wanted — the engine + Testing's server half are staging-only.**
## 08-01 THE CHECK-LIFE FIX IS LIVE ON STAGING (PM-verified, both rounds). **GATEKEEPER `src/calls/check-life.ts` + table `check_life`**: alive/verdict questions answer from OUR record + the carrier, state in the DB (hard cap 30 min = lost-callback backstop, NEVER a call timer). Mirror = `setLifeHook` (events.ts, pure); the carrier's end is ALSO stamped directly in `/twiml/bridge-status`, the only witness after a restart. **EVERY finalize asks `isCheckAlive(row.room ?? row.providerCallId)` — never the provider, never with nothing.** The room is minted and written on the row BEFORE the dial on EVERY path (`placeBridgeCall opts.room`), or the gates ask about null and charge mid-hold. **TRAPS: the `direct:` lane is the provider's own line so the gatekeeper defers to it there; a pre-written row needs its `roomFinalizers` hook or an unanswered check sits on `dialing` forever; `normSaid` dedupes at record/relay/page and the page repaints only on change.**
## 08-01 RUN 6's THREE FAULTS, ALL FIXED + SHIPPED (his screenshot: one line reading "Hi, do you recall Fun Store? This is Bob. Um, I'm sorry, we don't today"). **THE ROOT: held audio was handed over in ONE BURST. The transcriber decides where a sentence ends by hearing a pause pass on a REAL clock, so a burst has no pauses in it at all — wrong words AND two turns welded into one. Injecting silence into a burst does NOTHING (shipped twice, changed nothing).** Fix: Charlie's EARS open when a person is found and the line flows live; anything still held is paced at 20ms a frame and catches up when the backlog is small. His MOUTH stays shut until the question ends, and that gate is ALSO what picks the joining agent. `preRoll` keeps the line CONTIGUOUS once a voice starts (keeping only loud frames deletes the pauses INSIDE a sentence and slurs it). The page takes our saved order once it holds as many LINES. **`scripts/what-happened.mjs` reads a REAL check's record — run it FIRST on any transcript fault (RULES 12).**
## 08-02 THE ROBOT STORE IS LIVE ON STAGING AND ALL TEN SCENES HAVE RUN FOR REAL (spec `docs/specs/robot-store/README.md`; checks 235-252). **It is the SAME tape deck pointed the other way** — inbound scenes live IN `src/calls/tapedeck.ts` (`robotAnswer`/`robotStep`, ten scenes + five greetings VERBATIM from `how-staff-actually-talk.md`), clips ride `clip-cache.ts` (`mp3Clip`, recorded ONCE), TwiML + audio on `/robot/*`, scene chosen by the `robot_scenario` setting ("7" or "7:2" pins the greeting) through `/api/admin/robot-store`. **NEVER write a second engine.** Number +1 424 484 7395 → `/robot/answer` on the staging service; MVP store 106362 points at it on BOTH envs (phone is on store-sync's never-sync list) and chain 121 is `ringsDirect` or the site greys it "Coming soon". Fun store untouched. Harness `scripts/robot-check.mjs` drives the REAL staging site, signs in as the owner (code 000000), and reads through `what-happened.mjs` (ONE reader). **TRAP: this sandbox's browser cannot open an encrypted connection at all** — the harness pipes it to the real site over plain HTTP; on a normal machine it goes straight there.
- **SCENE 3 HELD EVERYTHING, TWICE. SCENE 7 IS A COIN FLIP:** checks 248 and 257 are the SAME words minutes apart, scored **in_stock** and **no_clear_answer**. Not fixed; its own task. Every greeting in all ten landed FIRST and on its OWN line, live and written.
- **THE ONE BIG FAULT, ITS OWN TASK: after a hold, a transfer or a hang-up, NOTHING Staff says afterwards is written down.** Scene 8: "Yeah, we've got a few." after a 30s check → filed **left_on_hold**, the yes thrown away (check 249, the exact 135 failure). Scene 5: "Yeah, I did not see any" after the 45s walk → record says **nobody_answered** while the SCREEN said **In stock** (check 246; the timeline itself logs "Staff greeting" at 4s, hold at 27s, reconnected at 69s). Scene 9's "I can't hear you. Bye-bye." and scene 10's transfer lines: same, missing. Scenes 1/2/3/7 (answer straight away) lose nothing. **ALSO FOUND: one check can write FOUR rows** (238 + 239/240/241, one conversation id; the three extras have no room, a SHORTER transcript, and land 41s later — so anything reading the newest gets the unfinished one). And scene 4 welded two turns into ONE line: "Mm-hmm. Hello? No, we don't have any this, this shipment." **COST ~5c a check + ~1c the robot's own line, ~62c for all ten.** **08-02 OWNER'S FOUR SCREENSHOTS: CHARLIE NEVER SIGNS OFF AND NEVER ENDS THE CHECK.** Check 258: answer settled at 30s, Charlie quiet at 34s, dead air until the STORE hung up at 98s. The robot used to hang up ~10s after answering and HID the length; it now waits ~40s (`WAIT_OUT`), which is what exposed it. He also re-asks the same question when the reply does not answer it (agent `turn_timeout` 5s). Harness items 13/14/15 catch all three (goodbye · same question twice · dead air after the answer) and FAIL today — Echo owns the fix. **CEILINGS (PM 08-02, comp switches off EVERY existing brake): `ROBOT_MAX_DIALS` 12 a run in the harness + `robotStoreOverCap` on ALL FOUR check routes, 40 in a rolling day, fires ONLY for the robot's number (`ROBOT_DAILY_CAP`). The store is proved BY ITS NUMBER before anything dials and the tapped row must carry id 106362. `/robot/*` is STAGING ONLY (`robotStoreOn`) or a promote opens a public way to spend voice credit. Proven live: cap dropped to 20 against 28 → the check was refused, NO new row. Three lines are spec-approved, not corpus (scene 8's payoff, scene 10's second voice) — the banner says so.** **THE HARNESS IS PROVEN TO FAIL: bridge.ts's store-side audio was cut on purpose (staging knowingly broken 05:56-06:13 UTC, owner told first, `.unlock` used and DELETED); scene 3 went from all-held to two failures (the words missing, verdict `no_clear_answer` not `not_in_stock`), then held again on the put-back build (check 256). Byte-for-byte restored. TWO GENTLER BREAKS DID NOT FAIL IT and that is worth knowing: dropping the quiet frames only cost a full stop, and dropping EVERY SECOND FRAME still transcribed intact.**
## 08-01 EVENING — THE CHARLIE RECORD EXISTS: `docs/specs/charlie-behavior/README.md` is now THE law for how Charlie behaves (every log line, every pass/fail both sides, the money boundaries off real rates, his eight tests which had NEVER been written down and died with each chat). Work orders beside it, owner-approved line by line: `echo-build.md` (round 1, running) + `echo-build-round-2.md`. **NOTHING SHIPS TO STAGING UNTIL THE ROBOT STORE CAN RUN A CHECK** (owner's own idea: a second number that answers itself, so an agent tests instead of his phone; it MUST vary across his eight tests, and a run gets a HARD CAP on how many checks it dials). Caught writing it: **`holdMaxSeconds` OPENS Charlie to an empty line at 60s** (his own log, real money) · a phone put down on a counter is neither quiet nor music so Charlie keeps billing (fourth shape: a loud room) · `ivrMaxSeconds` is a dead Admin box · the machine-phrase check can still hang up on real Staff BEFORE any hold. Three numbers move to Admin ▸ App and MUST live in `call_tuning`, never `policy_json` (prod's policy stomps staging every 60s): Charlie talk 23s · hold cap 120s · silence-before-drop 6s (keep 6 until the robot store measures the floor: too long costs ~1.1¢ a drop, too short makes him choppy and choppy loses whole checks).
## 08-02 CHARLIE ROUND 2 — **MERGED TO STAGING** (`echo-build-round-2.md`; PM audit failed the first cut, all items fixed). **`holdMaxSeconds` DELETED, not shortened** — it never ended a wait, it OPENED Charlie to an empty line at 63s (his log). Gone from `policy.ts` shape + defaults too, so it cannot be set, persisted or synced prod→staging; `ivrMaxSeconds` (wired to NOTHING) the same. **WHO ENDED THE CHECK = subtraction**: `noteWeEnded`/`weEndedCheck` stamped at ALL our endings (dropCall · voicemail · both give-ups · ring give-up · Charlie's close · `/pub/bridge-hangup` · `zoneHangRoom` · `hangupTwilioCall(callSid, room)` · Admin's stop); `/twiml/bridge-status` writes "The store hung up on us" / "The check was disconnected" when it was not us. **`farEndGone` is set on Twilio's `stop` FIRST** (real Twilio sends it on a far-end hangup while our leg still reads open). **OUR OWN `timeLimitSec` rides into the bridge**: a stop at/after the cap is `time_cap`, measured FROM THE DIAL (the bridge's own socket opens later, much later behind a menu), 3s slack — else our accounting reads as the store hanging up.
- **VOICEMAIL GATE = THE STORE'S FIRST LINE ONLY (`storeHasSpoken`). OWNER RULING, README §8 item 6, DO NOT RE-LITIGATE.** Not the spec's `humanAtMs`: a voicemail's recorded voice is what TRIPS the person detector, so that gate deletes the bail on its one real case; the earlier `weHaveAsked` gate had the same hole via the clip. Both fail because "a person answered" cannot yet tell a person from a recording — **round 1's person test fixes that, then KEEP BOTH.**
- Item 4: `looksLikeAMenu` (prompts.ts — words, never the Ear) + disconnected-during-transfer. Item 6: `mayWriteVerdict` + `lineStillUp` deleted · `/pub/charge` ONE capped list, survives a restart · `warnIfCapTooLow` (void) · the delta-lane thinness stated, not implied. **FOUND + FIXED: the gatekeeper answered "not alive" whenever the DATABASE was unreachable** (every lookup throws → unknown name → permission to stamp a verdict mid-check); a check we hold is now answered from memory first.
- Driven: delta-clip **143** · prompts **108** · check-life 15 · call-events 72 · behaved 39 · listen-nav 43 · gates 9 · spec-gates ok · tint 34 · tsc clean, before AND after the merge. **NOT phone-verified, and Admin not opened to confirm the two boxes are gone.** NEXT (THE-ORDER-2026-08-02): PM audits the code cleanup and merges it, THEN round 1 + the three Admin numbers against the clean layout, THEN the Testing card.
## 08-03 MERGED TO STAGING + ADMIN SHIPPED. **BOTH CHARLIES PULLED FROM ELEVENLABS AND COMPARED:** joining 17,997 chars = original 17,521 byte for byte + 476 of `JOINING_RULE` on top · `{{ask_for_transfer}}` and the ask-ONCE rule in BOTH (was 16,807 without it) · joining `patient`/speculative false, original `normal` · first message empty on both · same model + 110 tokens.
## 08-03 THE TESTING SECTION — **ELEVEN ROWS LIVE** (`behaved.ts`, README §5 is the law). New keys: handed_to_charlie · question_recorded · warmed_up_in_time · right_department · goodbye_when_told_no · wrapped_up · spoke_their_language · charlie_ended_the_check. **RENAMED to the record:** Transfer requested → "Asked to be transferred", Re-asked after transfer → "Reacted to a new person". Three states printed as WORDS + a count line. **Row 4 never ticks when the department was wrong** (it hands the verdict to row 5). **Row 6 names the event** (after a transfer / after a wait / a wait nobody came back from). **RETROACTIVE:** `beforeWeWroteItDown` (no `detail.step` anywhere) → "This check ran before we started writing that down"; row 9 falls back to `signedOff(last line)`. Receipt now also returns `lines` (the conversation) on BOTH routes; the sheet draws it under the ladder with the one `bubbles()` renderer. ONE engine addition the card needed: `saysNobodyToTransfer` (prompts.ts) + the `nobody_to_transfer` step, only read AFTER he asked. **DRIVEN:** behaved 69 · `scripts/qa-testing-card.mjs` on the REAL Admin (token → cookie by curl, every browser request node-fetched, `redirect: follow` or the sandbox proxy 503s).
- **THE TEN SCENES RAN (26 checks total this session, ~60c).** NONE fully green, and 8 of 10 fail the SAME known thing: **"the check ends with a goodbye" — Charlie never signs off** (already staging's own task, commit 17b7419f, predates this merge). My row 9 reports it independently. Also re-seen, all known and NOT mine: scene 3 welds the greeting, and after a hold/transfer/hang-up nothing Staff says is written down (scenes 5/7/9/10 verdicts). Scene 6 (walks away, never comes back) failed only a UI bounce check.
## 08-03 CLOSEOUT (owner, five items, in the same branch). **NO COUNT OF RINGS EVER ENDS A CHECK** — `RINGS_UNANSWERED` deleted, `ringWaitTimer` armed at `firstRingAtMs`, cleared in `triggerConnect`; `ringWaitSeconds` 90. **`maxCheckSeconds` 240 replaces `pol.bail.maxCallSeconds`** as the check length (bridge-place `capSecs` → ctx + TwiML TimeLimit; callers pass `v.maxTalk ?? undefined`; slot TTLs follow it). **`departmentName`** = last spoken word of `opts.say`, read in bridge-place BEFORE the route is consumed → "Transferring to front" / "Transferring you to the Staff."; "desk" is gone. **THE TWO CHARLIES: `JOINING_RULE` + `joiningPrompt` + `midCallAgentPatch(llm)` live in prompts.ts, imported by service.ts AND make-midcall-agent.ts**; every `pushPrompt` now writes the joining agent too (he was frozen 07-28, missing the whole `{{ask_for_transfer}}` section — 17,521 vs 16,807 — which IS the asking-twice fault). Drift alarm in test-prompts. **FOUND DRIVING IT: the tone window survived a whole ring cadence**, so a person answering in the gap was judged against a mostly-ringing window and never opened Charlie — `BURST_END_FRAMES` 25 clears `loudE/loudT/voiced` after half a second of quiet. Numbers in Admin are FIVE.
## 08-03 CHARLIE ROUND 1 — ALL EIGHT ENGINE ITEMS + THE THREE ADMIN NUMBERS, BUILT ON `claude/charlie-engine-tuning-zoxb4r`, **NOT MERGED** (owner's word first). `echo-build.md` PART 1 + PART 2 only; PART 4 (the card) is a fresh chat after Mapper's screens land.
- **1.1 the person test opens Charlie** (`looksLikeAPerson`, the SAME function, fed from the bridge): talking time accumulates across breaths, a real pause of `personWaitMs` closes it. Three things it dragged in: the clip no longer restarts its own greeting-end wait (`waitQuietMs = storeQuietMs`), `ConversationEar.heardAlready(voiceMs, closeLevel)` hands the ear the greeting it was attached too late to hear (or "nobody left, nobody was here"), and the deciding silence is trimmed off the preRoll handover. `greetingKeepMs` 6s→10s (the window must cover greeting + the pause).
- **1.2 the loud room** = 4th `HoldReason` "room": `closeLevel` is a decaying peak of the person's own frames (seeded from `storeLoud`, NOT `loudE` — that is cleared before the test fires), room = the last 300ms of sound below `roomFraction` (0.35) of it. Per-frame was wrong: real speech dips inside one word.
- **1.3** four notes with `detail.step` (`question_clip` · `prewarm` · `wrap_up` · `language`) — the sixteen kinds stay sixteen; `warmedUpInTime`/`deadAirMs` ride the join. `staffName`/`wrappedUp`/`usedTheirName` in prompts.ts; language via mapgraph's `guessLanguage`, and ONLY when Spanish appears (it answers "cannot tell" on plenty of English). **1.4** `question_live` on both no-clip doors. **1.5** `WRAP_UP_MS` off `speakingMs`, ONE contextual_update, never a hangup. **1.6** `holdCapTimer` armed in beginHold/cleared in endHold + `weHungUpOnAHold(room)` reads `call_events` at finalize so "left on hold" survives a restart. **1.7/1.8** "Charlie left" → "Charlie ended the check" only when it was ours; per-ring line deleted, the 6-ring rule kept.
- **THE FAULT THE RING SCENE FOUND:** the front of every ring burst lands in the person test before there are enough samples to call it a tone, and the gaps between rings accumulated as quiet — six rings opened Charlie onto a desk nobody answered. A positive tone verdict now wipes `storeSpeaking/storeTalkMs/storeQuietMs`.
- **PART 2: `/api/call-tuning` GET+PATCH → `call_tuning` ONLY** (never `policy_json`: the prod mirror stomps it in 60s). Out of range is REFUSED, not saved (the engine would drop it to the default). Admin ▸ App, three `.peek.static` rows through `envApi`, `loadCallTuning` in TAB_LOADERS.
- **DRIVEN:** delta-clip **193** (11 new scenes) · listen-nav 51 · prompts 129 · settings-sync 34 (incl. a full prod mirror leaving the numbers alone) · dropped-call 21 · behaved 39 · call-events 72 · `qa-admin-call-tuning.mjs` in a real browser, all pass · tsc clean. **NOT phone-verified and NOT robot-verified: the harness drives the real staging site, so it cannot see this branch until it merges.**
- **THE RIG'S "TALKING" MUST SWING** (`SPEECH(i)`): constant-amplitude LOUD reads as the network's own tone after ~40 frames, which is the engine's own rule, so long runs of it now test ringback by accident.
## 08-01 PM AUDIT: PASSED both rounds (5 blind readers + full rig, no fake fixes). Residuals, none blocking, all in `echo-build-round-2.md`: voicemail regex can bail on a human PRE-hold · `mayWriteVerdict` + `lineStillUp` dead · delta lane never stamps `check_life` · `/pub/charge` dedupe memory-only · `LIFE_HARD_CAP` unvalidated against a raised `maxCallSeconds`.
## THE ONE EAR — **a `PickupEar` was built here and DELETED the same night; §10 says never build a second.** A **VETO, never a green light**; anchors come off its own recording count. **PM: bail's else-branch blind-joins Charlie on a stopwatch if bail is off** (locked bridge). **`.unlock` WORKS**: the sprawl gate only hooks Write, so `printf 'src/voice/**\n' > .unlock` opens it; fix that scope ONLY, then DELETE it.
## 07-31 REBUILD RESTART — owner rounds are LAW (`build-contract.md` addendum R1-R6). THREE chunks, fresh
chat each (1 engine · 2 self-healing · 3 screens); PM audits each vs the contract on the owner's "finished"
before the next; owner tests ONCE at the end (fresh CVS start to finish). `audit-code-vs-contract.md` = the fix
list; old "done" claims VOID. PM re-proves run-resume live in the chunk-1 audit (did the 07-31 dead runs predate the fix?). Railway: `deploymentCancel` errors once, retry lands.
- **DRIVEN 07-31:** 207 map-sim · tsc clean · chain 5 clean on both envs over live HTTP. **REAL CHECKS 07-30/31:** Victory Blvd walked clean (graded pass, 47s); the night check failed and touched nothing; the map-stage morning check reached Staff who answered about Pokémon before a deploy killed the run.
- **STILL OPEN:** §11 cost per delivered answer · real `holdSeconds` > 0 · 7.12 · the save with a real human.
**HELD FOR THE OWNER:** live customer checks still fire recipes on elapsed seconds (menu-words nav built, defaults off) and their records call dial-to-person "nav" — flipping it changes the cost buckets, his call. The two whole-journey rig failures assert the OLD wait-for-approval behaviour R2 retires. **STILL SOURCE-TEXT (out of scope then):** voice-judge · map-sim · mapper-resume. **NOTED, NOT FIXED:** on a check walking a saved route the plan branch answers a prompt that reads as a question BEFORE the person judge is consulted, so Staff answering early on a mapped route can be pressed at — PM: decide in phase 3+.
**Phase 1, closed (08-01).** Charlie joins on the check's OWN record; graded when it ends, off his report. The department is his word (wrong department → redirect; otherwise Staff engaged → answered), never the in-stock reading. A mapping check NEVER takes a transfer (`neverTakeAHandover`, owner-named `src/voice/**` unlock, opened and deleted in that task) — customer checks still ride them, byte-identical. Seven practice checks written first and failing first; the leftovers (`GOING_TO_LOOK`, `SENT_AWAY`, `HUMAN_RE`, `LIVE_HUMAN_RE`, `looksLikeLivePerson`, `REDIRECT_RE`) are DELETED, and `navigator._test` lets a scripted check drive the real engine.
- WHY a mapping check never rides a hand-over: a good answer off a desk we cannot name or route to made mapping lock the wrong department. `setMappingHandoff` → `neverTakeAHandover`, read at the ONE place in bridge.ts that sets `expectHandover` off Staff's own offer; absent = every customer check, byte-identical. NOT DRIVEN: that branch needs a live provider connection; both its inputs are.
**PHASE 2 — SELF-HEALING (08-02, `src/calls/healing.ts`, driven by `test-healing.ts`).** New `retailers.muted/mutedReason/mutedAt` — NEVER `active`, NEVER SYNCED (a staging push would un-mute live stores; each side heals its own). Hand Mute/Unmute in Admin stores, `POST /api/stores/mute`. A customer check whose held route led to nobody → `learnFromReceipt` reports up, the store mutes + files ONE job; THE MUTED STORE IS THE JOB. Muted = off the consumer list AND its own `/pub/store/:id`, stock history, best-bet, rural fallback; the dial itself refuses (one guard); zone quote/run skip it with the reason; both schedulers skip; `autoCheckPaused` fires (customer · store · reason) with NO email copy. Re-map succeeds → `lockStore` unmutes, closes the filing, writes the ONE history line; no new status word. Three stores off = the chain's menu changed: never mute the chain, clear its shortcut so checks use full words, one run per chain. `healOnce()` is registered beside the other self-running ticks (`withLock("healing")`, 10 min) inside the mapper's daily cap. "needs review" gone everywhere (label is now `not proven`).
**RING FIX (08-02, owner):** the ring is EVIDENCE, not an override — it no longer answers ahead of the store's remembered lines, so a desk that rings out and drops us back into the menu is caught (practice check 4b drives it) instead of opening Charlie onto a recording.
**PHASE 2 AUDIT FIXES (08-02):** `healOnce()` wired into the self-running ticks (above), and the R3 sweep finished across every single-store consumer path — driven end to end in test-map-api. Also fixed: spec gate 2 piped a search into `grep -q`, whose early exit SIGPIPEd the feeder under `pipefail`, so a correct push was blocked at random; it counts now.
**PHASE 3, FIRST PASS (08-03, screens only — engine/healing/`src/voice` untouched).** Chains list: the red alert icon and the "Menu changed" choice now light off `graphSummary().menuChanged` (a read of the same `muted` flag) — driven on staging: mute CVS Mulholland → CVS flags, its own page 404s, unmute → the list empties itself. Mapped checks: **"Recipe winner" is RETIRED** (Update 5 overrides comp 3a) — the winner lives in the recipe box and Menu; a passing check states what it did, green ONLY for a confirmed right department. Menu: a choice proven wrong is struck through in red (comp 3b) off the new `chainDetail.deadDoors`. The retired review word is translated on READ, so rows saved before the rename can never print it. **TWO FACTS ADDED to the server, both reads of state that already existed:** `menuChanged` on the chains rows and `deadDoors` on the chain detail. **PHASE 3, SECOND PASS (08-03).** OWNER RULING: the FIRST check of a run proves the department (it walks the whole menu AND asks Staff), so it is headed **Proving department**; the listening checks after it are Mapping menu, the speed tries Optimizing speed. Driven off `askedStaff` (the run's own `confirm`, newly handed to the page). Also in: the three report boxes on the chains page (mapped · unmapped · menu changed, each taps through to the filter), the filter row forced to ONE line, and an "are you sure" before a Menu wording correction saves (R5; what we heard is kept underneath). **`POST /api/admin/map/simulate` — STAGING ONLY, refused on prod** — writes one mapping run in exactly the shape a real check writes (proving check · settle listen · a failed speed try with its reason · a speed win) so every screen state can be looked at with no phone and no money. **PHASE 3 AUDIT FIXES (08-03):** the pencil is DELETED (R5) — a Menu line is a dashed box you tap, save beside it, confirm before it lands. **R5's ONE TIMELINE per run is built** (`runTimelineHtml`): the checks that moved us forward are rungs with their heading, time and gain; a check that changed nothing is a small dot carrying its reason, tappable. Every made-up check now SAYS SO on both the timeline and its card (`madeUp` rides the check, not the store name). Correction to my earlier note: the heading is not "the first check of a run" — ANY check that reached a person and asked about the cards is Proving department (it can take several at one store), a check that never got to ask is Mapping menu, and everything after the department is proven is Optimizing speed. The code was always right; the note was wrong.
**DRIVEN 08-02 (after the audit):** healing **47/47** · practice-checks 46/46 · voice-judge 48/48 · listen-nav 43/43 · map-sim 331/331 · resume 20/20 · mapgraph 62/62 · map-api **20/20** · bridge 13/13 · map-e2e 100/102 (the two known) · tsc clean.
NOT verified: a real phone call — none of this has run against a live store. Side branch `claude/mapping-engine-contract-wecvcy` only; NOTHING merges to staging (the owner is testing; a restart kills a live check).
## 08-02 VOICE MODEL RULED BY THE OWNER: **STAY ON `eleven_turbo_v2`.** He heard Branson on turbo_v2 / turbo_v2_5 / flash_v2_5, same line and settings: v2 the most natural, flash slower-sounding, 2.5 "more of a younger kid". **COST IS IDENTICAL** across turbo v2 / turbo v2.5 / flash v2 / flash v2.5 (all x0.5); only `eleven_v3` + `eleven_multilingual_v2` are x1.0. **A LANGUAGE NEVER SWITCHES THE MODEL and never changes the bill** — the agent has NO `language_presets`, so Spanish comes out of turbo_v2 with an American accent (that is the Spanish he has already approved by ear). The reason to revisit 2.5 is GOING GLOBAL, nothing else. Admin's Designer dropdown offers only turbo v2 + flash v2, so a x1.0 model cannot be picked by accident.
## Engine — Branson HD `1P1JhCcLzeMmkvLi1BkG` speed 0.91 (BOTH envs) · echo gate 520/150 · ringback by published
tone frequencies, 6 rings = hang up · **Charlie $0.00183/s, meters SILENCE** · Twilio WHOLE MINUTES · 5.2c base ·
**all FOUR dial paths open a receipt** (bridge-place · navigator · tapedeck · native `direct:<id>`). Traps: never
run the full suite for a small change; never deploy mid test check; `/api/*` is admin-gated whole (a THIRD-PARTY
route must be exempted or it 401s silently); auto-nav 0-hammers when it cannot parse → FALSE "no human"; old
whole-call Delta STAYS in the tree.
