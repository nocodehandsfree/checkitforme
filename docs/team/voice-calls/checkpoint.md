# VOICE-CALLS — checkpoint (current state)
> Calling engine + voice tuning + phone-tree mapping. `src/voice/` is FROZEN (machine-locked; owner
> task + repo-root `.unlock`). Charter: handoff.md + MAPPING-MANUAL.md. <=60 lines. **LAW: Admin is the record of truth.**
## 08-20 OUR OWN BRAIN, BUILT AND DIALED (owner's go; full detail in `echo-handoff.md`). NOT the blocked road: the provider refuses a custom brain on any agent with a quick voice clone, so WE run the conversation and their PLAIN SPEECH service says each line in the same voice, the machinery the fresh "Oh, hey!" already uses. `src/voice/ourbrain-session.ts` speaks the provider's own message protocol, so `bridge.ts` cannot tell the lanes apart and every rule in it runs unchanged; `wireTheSession()` is the one place both are wired. Echo is the whole of his hearing here, so every fresh Staff line is handed as their turn (never their greeting, never their walking-away line). Fallback wired: `onStumble` -> `brainFellBack` -> the hosted agent mid call, same voice, nothing said out loud. `charliePaidSeconds` (events.ts) is what the cost engine prices, so a stretch our brain ran costs nothing per second. **CHECK 427: FAILED on ONE red row (awake on hold 7s), everything else green or yellow. 2.8¢ against 9.5-11.9¢ the old way, profit 89%, meter 31s real / 27s graded under the 31 line, answer gap 2s GREEN (was 7s red), Echo handover 1s green, card verdict PASS, and the harness said EVERY SCENARIO HELD — including the 08-07 screen red.** First sound 4.1s after they came back; full reply 1.4s after their words ended; every reply stamped `brain_reply` with the model and both halves of the time. **08-20 THE SILENCE FAULT IS FIXED (Echo 4, commit 18030a8 on staging).** On 427 the wake check proved a person 154ms BEFORE the sound of the reply our brain had already written, and `releaseTheEarlyTurn` threw the turn away at that moment with his words in it: no line on the sheet, none on the customer's page, no `brain_reply` row, and the frames that landed a beat later went out orphaned behind "Oh, hey!" — sound the store heard with nothing on the record to say what he said. The turn is HELD now until the first frame of its sound arrives, then goes out whole through the one release door and writes its line there; a frame landing on a proved turn whose words are in joins that turn (audio door); `stampWhoWroteIt` is the ONE source of the brain stamp so a held reply can never lose it; `EARLY_STALE_MS` (3s) is the ceiling on that wait. **PROVEN ON THE WORKBENCH BEFORE ANY DIAL:** the rig's new CHECK 427'S MOMENT scene drives the order no other scene had (words, then the proof, then the sound — its neighbour hands words and sound together, the order that always worked) and fails 3 of its 12 on the old code, all 12 on the fix. **08-20 THE OWNER WIDENED THE AWAKE BANDS:** green to 3, yellow to 8, red from 9 (`AWAKE_ON_HOLD_YELLOW` 5 -> 8, meter.ts), true seconds only — his head start costs ~3.6 awake seconds by design, so 415/423/424/427 all sat on the old edge. **THE DIAL IS NOT RUN YET: Railway had three builds QUEUED 25+ minutes and staging still serves 0f93e733, so nothing of this is live and the ordered scene 22 dial waits.** THE SWITCH IS BACK OFF, test dials only. Suites: delta-clip 432 · meter 96 · ourbrain 31 · behaved 109 · call-events 124 · bridge 13 · tsc clean.
## 08-20 THE COMEBACK, SPLIT IN TWO + THE QUICK THANKS (owner's two fixes; full detail and every number in `echo-handoff.md`). **FIX 1** `HOLD_ACK_LINE` = `"Sure, thanks!"` / `"Claro, gracias."` — two words, played only as Staff walk away, 1.4s on the line (the old "No worries, take your time!" ran nearly two). **FIX 2 THE TWO PART COMEBACK**: the instant the words prove a person AND that person pauses, Charlie says `COMEBACK_HELLO_LINE` ("Oh, hey!") in his own voice, MADE FRESH EVERY CHECK and never kept (`phoneClip(..., fresh)` skips the clip store both ways — it is the one line of ours that is not a recording); his full reply keeps building behind it and the engine always plays the two words first. Started at the reconnect, binned untouched with his session when the words say the store played that at us, never over the person (`theirVoiceOn`) or our own audio (`agentPlayingUntil`). **ALSO BUILT THIS RUN**: the wake check reads Echo's first PIECE instead of the joined sentence (the wait now ends ~1.4s after their voice, was 3.6s) · his reply is HELD not dropped while it proves · a reply he began BEFORE their words reached him is thrown away (417/419: the store heard the hold reply as the answer) · a repeat of the hold reply is silenced · the wordless-rejoin guard no longer re-arms on a comeback words just proved (416: dropped mid reply, 84s/11.3¢) · the wait's own row is drawn before the hand-over row and its sentence reads those rows. **CHECK 424: FAILED**, meter 37s real / 30s graded (under 31, yellow), card verdict PASS, 11.1¢; **his first sound 3.6s after they came back** (1.9 of it their own sentence still being spoken), reds = awake on hold 6s and his answer gap 7s. **THE OWNER'S OPEN DECISION: his head start costs 4-7 awake-on-hold seconds by design, so that row swings yellow to red; either the bands move or the head start goes.** Proven first and pinned: hold-wake 26 (on his own advert recording) · delta-clip 407 · behaved 109 · prompts 262 · meter 93 · call-events 124 · listen-nav 78 · bridge 13 · dropped-call 33 · tsc clean.
## 08-19 ECHO 3, THE ADVERT GRADING FIX (owner ruled: grade such calls fairly after the fact; commits 9915c6c2 + b34dd95b, full story + the interpretation call for the PM in `echo-handoff.md`). **`advertAsWait` (meter.ts, pure): when `played_at_us` proves a recording, the GRADE treats the stretch as a wait** — hold from the `music_heard` stamp when one covers the judged line unbroken by a person (else the judged line itself, 376's family), drop the ruled 3s in, off until the real comeback, intersected with his real meter stretches. meterVerdict grades meter AND profit (a friend: the forgiven seconds are his billed seconds) on the as-if numbers, real numbers printed beside, forgiveness in its own ungraded row; `meterStoppedOnHold` passes with words when no live hold + the proof; a `gaps`/`missed_turn` stamp INSIDE the window does not grade (401's silenced-note reply read as a 14s answer gap). Record/cost/charge byte-identical; no proof = grades byte-identical (asserted). **PROVEN: 398/399/400 re-read live all TEST PASSED (46/47/49s red → 26/27/29 graded; 56/55/53% → 70/69/68 vs the 56 floor) · fresh dials 401 + 402 both scene 22, 402 PILL PASSED clean (26s graded, 70%).** Harness word-for-word red on both dials = the robot's once-only "Sorry, that's all I know." answered into Charlie's goodbye, unwritable by design, the 08-04 teach-the-matcher family. Suites: meter 86 · behaved 102 · simulations 18 · tsc clean. **TWO, THE RECORDED GOODBYE — DONE, proven on CHECK 405 (pill PASSED)**: his sign-off is the owner's ruled line "Thanks so much, have a good one!" (`GOODBYE_LINE`/`_ES`, charlie-setup.ts, per-voice phoneClip, NO name), played by `sayTheRecordedGoodbye` (bridge.ts, .unlock opened+deleted) at the engine-known moments — the reader's knock with NOTHING missing (first or re-knock), the warm wrap-up's quiet, and the fresh Staff line after OUR set-ask recording (guarded by `asksUsBack` + `saidGoingToCheck` so a question back or a walk-away is never goodbye'd) — closing through `hangUpAfterGoodbye`; his own versions drop behind `ackPlayingUntil` and are never written (`covered_by_recording`/`goodbye_covered`). TWO FAULTS THE DIALS CAUGHT, both fixed+pinned: 403 (warm wrap counted its 5s from ARMING, cut off the set answer → the quiet now counts from the last SOUND, ours or theirs) and 404 (his generated goodbye beat the reader's re-knock → the answer to the recording's question closes deterministically). 405: set-ask clip 41.3s · answer ON record 47.7s · recorded goodbye 52.5s · signed off · meter 45 real/25 graded · profit 56 real/71 graded · 10.9¢. Rig 387 · prompts 268. **THREE, THE OWN BRAIN — STOPPED, WRITTEN UP (PM: unblock wanted)**: everything §7 orders already exists (switch · /pub/brain endpoint · ladder rungs · segmentBrain stamp · §8 call_dropped never-completed/never-charged) EXCEPT the agent — the provider RE-REFUSED today, custom LLM + Branson HD (instant clone) = `custom_llm_not_allowed_in_with_agent_with_ivc_voice`, same as 07-31. Ways out are the owner's: professional voice clone, or a premade voice (breaks his ruled voice). Finish line (loud music < 31s, switch ON) and §8's real-dropped-call proof both ride the unblock. Full detail in echo-handoff.md.
## THE LIVE CALL RUNTIME — all of `docs/specs/live-call-runtime/` built, ON for staging. READ THAT SPEC FIRST; this file is only traps + what is open. §3's six calls need HIS phone (`scripts/gate-zero.ts`).
## 08-18 THE LATE REJOIN IS DELETED (owner's ruling). `joinWhenTheyFinish`/`joinNow`/`JOIN_BACKSTOP_MS`/the `joined_at_their_pause` step are GONE; `endHold` calls `openHimForTheComeback` directly, so his session opens on their voice, not their sentence end. It cost the answer on the replays of 373 and 371 (their finished line landed while the session was opening). **STANDING LAW: no change to WHEN his ear switches on/off until the post-twenty-tests rebuild.** **AND THE 08-08 INVERSION IS RESTORED**: the memory-only drop (`itIsTheirTurn()` in beginHold's gate) made ANY quiet a wait while the answer was owed = a person thinking, and broke 3 old shapes; the gate reads `waitAnnounced` again and the ledger only ever HOLDS HIM ON (in `armTheMeterOff`). **OPEN, owner's call: check 376's Spanish announce never drops him** — the wordless version cannot be told from a thinking pause; the honest fix is the READER reporting "they stepped away" (a model, not a word list). Named in `scripts/test-delta-clip.ts` at CHECK 376'S SHAPE. Robot store says its one honest line in the voice of whoever is on the call (staff clip at `acts.length+1`, transfer at `+2`). **08-18 DAY — TESTS FIVE/SIX/SEVEN REHEARSED (checks 378-390, branch claude/echo-test-five-music-hold, all merged to staging; full detail + second-by-second floor math in `docs/team/voice-calls/pm-report.md`). FOUR FIXES, each rig-proven then dialed:** (1) CHECK 377'S EAR FAULT: on a hold, a run of sound banks as comeback evidence only when it BREAKS at word scale; an unbroken run past 1.2s is struck as the music (`MAX_SPEECH_RUN_MS`, ConversationEar) — measured off 378's own tape: music held 100% loud for 14 straight seconds, speech never passes ~2/3 loud in a second; loudness SWING separates NOTHING (music swings 600→6500 inside seconds, exactly like a voice — a swing prong flapped on 378 and was removed). The rig's raw SPEECH shape gained the silent fifth frame `speak` always had. (2) THE WORDLESS REJOIN (check 380, the waltz — on the line it plays at speech level with gaps, energy can never refuse it): a rejoin that writes NO words through Echo inside 4s (the writer's benched worst 3.16s) was the music → drops again AT ONCE (no min-on-line: nobody is there), and THAT hold reopens only on Echo's first written word, backdated to the ear's comeback moment (`REJOIN_WORDLESS_MS`/`heardTheWords`/`endHoldOnEvidence`, bridge; .unlock opened+deleted twice). (3) CHECK 386: "Let me put this down a sec and go check." was the one announce `saidGoingToCheck` missed → room hold sat behind the inversion gate; taught the matcher (bare `go check/look/see`, `put this/the phone down`), every hold scene's announce pinned in test-prompts. (4) CHECK 389 (test seven): the robot's hangup beat the 120s hold cap by 2s AND the settle read the DB in the same ms the hangup row wrote → reader's no_clear_answer stood; new `diedOnAHold` (service.ts, hold_start with no later hold_end — committed minutes before the ending, race-immune) → left_on_hold whoever hung up. **PILLS: scene 21 PASSED (382: meter 27y, profit 70, 7.4¢) · scene 24 PASSED (387: 29y, 68%) · scene 20 FLOOR (379/384: meter 39-43 red = 6s music proof [the drop-gap row can NEVER be green on a music hold: green 3, proof 6 — grading vs law collision, HIS call] + provider swing 2-7s/turn, benched) · scene 23 floor family (385/388: behavior perfect, meter 35-45 = provider swing coin flip; its twin 387 passed) · scene 22 OWNER'S CALL (383: ad-mix measures as a VOICE on the line and writes REAL words; no sound rule and no wordless test can refuse it — needs the READER judging the words, same family as 376's Spanish announce below) · scene 6 PASSED (391: left_on_hold, meter 10s green — doubles as live proof of the three-door statusFromTheRecord fix).** Suites: bank 347/0 (new scenes: 377's dip ×3 ear shapes · CHECK 380'S SHAPE) · listen-nav 68 · dropped-call 33 · prompts 226 · bridge 13 · call-events 124 · behaved 99 · meter 65 · clips 10 · gates 9 · tsc clean · spec 6 reds (baseline 7).
## 08-15 TESTS 1-4 PASSED (362 clear yes · 364 vague yes · 366 Hold: silence, scene 5; scene 11 of test four NOT dialed, owner's go; owner runs the walk, two tests per order, rule 15 binds; `docs/specs/testing-page/THE-WALK.md` is his page). Check 366 doubled as live proof of the fix below: quiet 24s, drop at exactly 30s (the Admin 6), back after 44s, sheet verdict pass. Two known reds on the harness, neither the card's: the customer screen said In stock against a not_in_stock record (the 08-07 list fault, also 301) and word-for-word flagged dropped "uh" fillers (the 08-04 "151" ruling: teach the matcher). **THE HOLD DROP SWITCH IS FIXED (owner task, off check 364): the carrier socket connects BARE, the room only arrives in the start message (the 08-04 goodbye shape), and bridge.ts resolved `tune` at CONNECT — `contexts.get("")` → `TUNING_DEFAULTS` — so EVERY real check ran the code defaults and Admin's `call_tuning` never reached the ear: 364 dropped Charlie at 3s (the default) inside a 5s quiet against the saved 6. Fix: `tune` is a mutable COPY and `adoptTuning()` re-derives ALL eleven derived numbers (wrap-up · hold cap · ring wait · min-on-line · thinking · greeting end/max/keep · clip settle/backstop · prewarm) at connect AND from the start handler — the same two moments `hangSignoffDoor` runs. Rig scene "bare carrier socket obeys the 6" in test-delta-clip: FAILS on the old engine, 283 pass on the new; bridge 13, dropped-call 28, tsc clean. Saved 6 untouched. Test 4's record doubles as live proof: the drop must land 6s after the quiet begins.** **08-15 EVENING — THE RECONNECT FIX (round two of the ladder, owner's go, off 366's 9 silent seconds 69→78):** heard is recorded AT THE HANDOVER (`hisAlready` marks ONLY in `handTheirTurn` and at his own session's `user_transcript`; the old eleven-open-at-Echo-landing inference is DELETED — it filed 366's no as heard when his session opened after it was spoken). The stale pocket DIES at every join (`missedWhileClosed` cleared at first-join ready — 366's hello from second 3 sat in it all call — and consumed at reopens). A line SPOKEN before `hisEarsBackAtMs` (stamped where the reopen commits, which is where audio buffering resumes) that lands after ready is handed at once through `handTheirTurn` (ONE door for pocket + late lines; its wrapper carries the ladder's words: ask only the missing piece, nothing missing = wrap up; lands in the pocket instead when the session is not ready yet). Rig scene "CHECK 366'S SHAPE" (stale hello never rides the reconnect · the late no IS handed): FAILS on the old engine, 289 pass on the new; bridge 13 · dropped-call 28 · call-events 102 · tsc clean. **BENCH, report only: the provider CANNOT lower the 5s turn wait per turn or per conversation** — the agent's own override switchboard (`platform_settings.overrides.conversation_config_override`) exposes only `turn.soft_timeout_config.message`, no `turn_timeout`/`turn_eagerness` anywhere, and no client event changes config mid-session (config reads once at session open); nearest door if wanted = a TWIN agent with a shorter wait that only reopened sessions use (NOT built). RE-PROVEN LIVE on check 367 (owner ordered four again): missed_turn at 74.84s carries THE NO with why="the line finished writing after he reconnected", Charlie spoke 1.9s later (76.7s vs 366's 78.1s), sheet verdict pass, 13.3¢. NEXT BOTTLENECK, measured: Echo's WRITING — the comeback line is spoken 65.2s to ~70.5s and lands written at 74.8s (~3s past utterance end; stitcher+Deepgram finalization), so the hand-over is instant but starves on the words. Same two known harness reds (screen In-stock-vs-record, word-for-word "uh" fillers). Owner's next: test five; the PM's test-one re-prove is still open, his call. **08-16 THE RECONNECT WORDS (PM box, round two): the ear opens his turn now.** BENCH (6/6 still, new split columns in deepgram-bench): ear voice-stop → last final piece 520-840ms · last piece → joined line a fixed ~1000ms (the utterance_end wait), so today's engine waits ~1.6s past the sound on the bench and ~4s live. BUILD: transcriber.ts gains `onPiece` (each is_final piece, never recorded); bridge gains `pieceDoors`/`echoHeardPiece` → `staffPiece` collects `openTurnPieces` (cleared on EVERY joined fromEcho line — the joined line replaces its pieces); at a words-only reopen on an Echo room `reconnectFeed` opens, `charlieMaySpeak=false`, and the turn OPENS on the EAR's voice-stop (`theirVoiceOn` flip, the existing tracker — never a second detector, never the writer's 1s quiet): `handPiecesNow` hands unhanded pieces through `handTheirTurn` (marked heard at the hand, the 08-15 door), stragglers hand on arrival, ANSWER_WAIT 9s backstop reused, the voice-START opener is suppressed during the feed (they are mid sentence at a reopen). The joined line: tail pieces hand first, then keyOf(joined)==keyOf(handed) → markAsHis + absorb, NEVER re-handed (the stale-hello mirror). Rig scene "THE RECONNECT FEED" (7 asserts: held while voice on · handed at ear stop · one turn oldest-first · joined absorbed · record one whole line): 296 pass · bridge 13 · dropped-call 28 · call-events 102 · tsc clean. DIALED on the owner's go: check 368, sheet PASS, not_in_stock. missed_turn at 69.04s, lines:2, why="the ear heard their voice stop" (367 had 74.84s) — 5.8s earlier; Staff's voice ended ~68s so the hand came ~1s after the sound. Spoke 73.6s, ended 91s, meter 43s (was 46), 12.7¢ (was 13.3). Word-for-word red this dial also flagged a dropped "I" ("Did not see any") — the 08-04 teach-the-matcher shape, line whole on the record. Owner's next: test five; scene 11 + the test-one re-prove still open.** **08-16 LATE — THE FOUR COST FIXES (PM box, off 368):** (1) On Echo checks the session NO LONGER opens at person-found (the 08-01 early-open existed because his session was the only transcriber; Echo owns words now): triggerConnect's clip path skips connectEleven on echoRooms, the prewarm (default cut 2000→800 in tuning.ts) opens it ~clip end, the hello hands as Echo's written `theirFirstLine` (helloLineWaiting, markAsHis, NO second STT call), and a real answer said DURING the clip rides the POCKET, handed at first-join ready (the echo window drops store frames under our clip, so words are the only carrier; pocket push gains `|| !ready`). Non-Echo checks keep the early open. (2) MEASURED: the 368 comeback 66→69 = 1.8s Staff still talking + 0.8s ear confirm + 0.24s ours — the wait past voice-stop is ALREADY ~1s (bench: stop→last piece 520-840ms, join +1000ms). (3) Admin "Silence before Charlie drops" SAVED 6→3 via PATCH /api/call-tuning, read back 3. (4) `personWaitForStore` (charlie-setup, pure, tested): known-direct chains (`ringsDirect`/`answerPath=direct_human`, threaded buildRestockVars→placeBridgeCall→setup as `directPickup`, both dial paths) wait 1500ms not 2500 before the question; the greeting-length test and the words voicemail bail untouched. Rig scene "FIX 1" (no session 1s into the clip · opens on prewarm · during-clip answer handed as their turn · hello never rides · STT_CALLS 0): 304 pass · bridge 13 · dropped-call 28 · call-events 102 · listen-nav 59 · tsc clean. DIALED (owner's go): check 369 — sheet FAIL, missing "Meter stopped", though verdict not_in_stock and 12.3¢ (meter 40s; fix 1 live: clip 7s, join 12s; drop at 27 = quiet 24 + the new Admin 3). ROOT, fixed same session: `waitAnnounced` outlived its wait — the 12s announce stayed armed until Staff's next WRITTEN line, Echo's writing runs seconds behind, and with the switch at 3 Charlie's post-comeback thinking gap became a FALSE second hold (72→77) failing the row; AND the joined comeback missed its absorb (the false hold nulled `reconnectFeed`) so the pocket re-handed the same no at 77.31. Fixes: `endHold` spends the announcement (`waitAnnounced = false`); `handPiecesNow` re-decides `waitAnnounced` from the handed words AND marks the running join as his AFTER the hand (single-piece turns must not self-filter). Rig: feed scene + "the quiet after the handed turn is him thinking, never a second announced wait" — 305 pass · bridge 13 · dropped-call 28 · call-events 102 · tsc clean. RE-DIALED: check 370 — sheet PASS, nothing missing, not_in_stock, 11.7¢, meter 37s (366 was 13.4¢/46s: 1.7¢ and 9s off). Clip 7s/join 11s (no meter under the question) · drop 26 = quiet 23 + the Admin 3 · ONE hand at 76, no false hold, no double-hand · ended 91s. Only the two known harness reds. **08-16 THE EARLY QUESTION ON 370 (answer A, proven by the stamps):** the set question was SPOKEN at 12.7s, before Staff's first answer — the 10.73s missed_turn carried THE HELLO ("Larry Vásquez…"). ROOT: fix 4's 1500ms wait commits the clip ~7.06s, Echo's writing of the hello lands ~a second behind the sound, so on 370 the words-hello branch found `theirFirstLine` null, no marking, and the pocket handed the greeting at first-join ready as a turn he owed. FIXES: `wantHelloFromEcho` + `clipCommittedAtMs` — when the hello's writing loses the race, `staffSaid` finishes the hand-over the moment the FIRST line lands, marked his FIRST, and ONLY a line spoken before the commit qualifies (a first line spoken after the commit is conversation — the 360 rig scene caught that hazard); `tellCharlieWhatHeMissed` drops `theirFirstLine` by key defensively; Echo checks NEVER fall back to the paid second transcription. Rig: the FIX 1 scene now lands the hello late and backdated, asserts it never rides any hand-over — 305 pass · bridge 13 · dropped-call 28 · call-events 102 · meter 25 (the new agent's suite) · tsc clean. ANSWERED, NOT FIXED, owner rules: "No worries, take your time" is gone on 369/370 because the announce-drop at 3s beats the provider's own reply (its turn wait runs ~2-4s); on 368 the 6s window let it land at 23.17. Owner's options: accept silent walk-aways · raise the switch · the own-brain plan. RE-DIALED CLEAN: check 371 — sheet PASS, not_in_stock, 13.0¢, meter 44s. Record order RIGHT: greeting 4.12 · question 7.88 · answer 13.09 · "No worries, take your time!" 27.78 (it LANDED this dial: their long answer arrived as words at 13.09, he replied before the quiet, and the close waited for his pending word — drop 29.52) · comeback handed 73.58+73.70 (the feed + its straggler door, both pieces the no, NO hello) · goodbye 94.2. Same two known harness reds only. **08-16 LATEST BOX (off 371, WHICH THE SHEET GRADED FAILED — meter 44s red at 31+ and profit 48% under 67; my report said passed off the CARD half. NEVER AGAIN: the report wears the PILL'S word).** BUILT: (2) `holdAckClip`/`Es` (charlie-setup HOLD_ACK_LINE/_ES, phoneClip-cached; bridge `playHoldAck` at the fresh announce in staffSaid: frames down the line at once, recorded+relayed as Agent, speakingMs+wrap counted, `spokeThisSession=true` so the drop counts from THEIR quiet, `ackPlayingUntil` drops his own late generated version, cleared by any fresh non-announce line). (3) post-stop pieces COALESCE 300ms (`pieceCoalesceTimer`) → late writing hands ONE turn (371 handed two 0.12s apart). (4) the PILL NAMES ITS KILL: meter.ts `shortFails` + app.html verdict.why (first of: missing row · wrong status · meter short words) worn on the pill ("Test failed · Charlie meter time, 44 seconds"). (5) THE NAMED GAPS: engine stamps (`sinceVoiceStopMs` on missed_turn · `gaps` summary event with `answerGapWorstMs` at close · drop gap = leave−hold_start from the timeline), v2For reads them, meter.ts grades: answer gap green 2/yellow 6/red 7+ FAILS · handover green 1 (yellow to 3, red 4+ = one notch of grace, MY width, his eye) · drop green 3 (yellow to 5, red 6+, same grace) · rows render on the sheet under the tiles ("Where the seconds went"), a row only when measured. Rig scene "CHECK 371'S SHAPE" (ack plays at the announce · on record as Agent · drop on the switch · late pieces = ONE turn): 311 pass · meter 34 · behaved 99 · bridge 13 · dropped-call 28 · call-events 102 · tsc clean. DIALED: check 372 — THE PILL SAYS FAILED · profit 61% under the 67% floor (9.8¢ against the 25¢ price; the floor needs ≤8.25¢). Everything the box built WORKED on the record: hold_ack_clip at 20.65 (1625ms, en) the instant the announce landed · drop 23.70 = quiet 20.70 + exactly 3 · ONE handover at 68.75 (lines:2, one turn, coalesced) · handover gap 1s GREEN · drop gap 3s GREEN · meter 28s YELLOW passing · card pass, order right · cheapest hold check yet (366 was 13.4¢). **08-17 THE FIVE-ITEM BOX, BUILT, NOTHING DIALED (the silent hold test dials on the owner's order; the report wears the PILL's word).** (1) ONE CLOCK: `recordLine` (events.ts) is MONOTONIC — a backdated stamp CLAMPS to the last written line, never re-sorts (372's "Next week maybe?" arrived backdated 120ms under its question) — with ONE scoped exception kept on purpose: `earMeasured` (threaded as `!fromEcho` in staffSaid) lets a moment OUR ear measured (the held hello's voice start, owner 07-31) still file above; Echo's Deepgram stamps always clamp. BOTH laws pinned: call-events 109 (372's shape + the hello) · the rig's hello scenes stayed green. (2) the sheet PILL says only Test passed/failed; TAPPING it opens the reason (why + missing + status line in a hidden strip; the standalone red strip is gone). (3) "Where the seconds went" rows: THE OWNER'S OWN SENTENCES (his 08-17 reword, exact words asserted in test-meter): each row is `say{pre,num,post,tail}` + `open` on meter.ts rows, the number wears the tone color, the COLOR does the grading (no goals/colors/bands in a sentence), `tail` is the sentence's second half kept whole so a phone drops it to its own line in one piece; app.html renders the sentence with the colored number inline, tap opens `open` (plain, about THIS call); proven by screenshot at 390 and 375 (nothing breaks mid phrase). The head's sub also lost its `max-width:0;min-width:100%` trick (Safari can zero it: his one-word-per-line label) and takes the screen's width. (4) HEAR THE CALL: bridge-place dials now carry `Record:"true"` (mapping's 07-30 flag, every call we dial ourselves); the answered stamp carries `callSid` onto the record (bridge-status); NEW route `/api/admin/check-audio/:room` (live map → record's callSid → carrier recording, `?probe=1` answers exists), lucide volume-2 speaker in the sheet head (HE NAMED THE SET, the icon pick is mine — his eye), shown ONLY when the probe says a recording exists, so simulated checks and pre-flag checks show no player. (5) HOLD SET-ASIDE: `holdCostAside` on MeterBounds, set on hold_silence/music/phone_down; v2For prices the record's `holdSeconds` share of line+fork+stt (NEVER Charlie) off the check's own measured cost into `profitHoldAsidePct`; meter.ts grades THAT only when the card carries the flag — customer checks carry no card, floor untouched. ALSO FIXED: the answer-gap stamp now emits the moment the worst grows (bridge ~1919), so it can never lose the close race that dropped 372's row; the close-time stamp stays as backup. DIALED ON HIS ORDER: check 373 (scene 5, room 9dd3c94f) — **THE PILL SAYS PASSED**: card pass · not_in_stock · meter 29s yellow · waiting 13s · answer gap 4s yellow (the at-measure stamps wrote at 75s and 83s: the close-race fix proven live) · handover 1s green · drop 3s green · profit graded with the scene's 44s hold set aside. **OWNER SETTLED 08-17 EVENING, NOT BUILT (rides the next box): the hold set-aside comes OUT entirely — "we already have a system, I didn't ask to change shit". Remove holdCostAside + the aside math + the 70% sentence; the sheet shows and grades ONLY the real profit against the 67 floor, the system as it was. KNOWN CONSEQUENCE, owner told: 373 re-grades on every read, so its pill flips to FAILED at 60 once the aside is gone. AND the Testing page's big number syncs with the check numbers MOVING FORWARD, no renumbering history ("when you say 374 I look and there's been 374 tests"). AND the call tape does NOT die at a hold (the PM's step-1 premise was wrong): 373's carrier recording holds ~79s of its 84s, the whole hold on it; only checks dialed after the 08-17 afternoon Record flag have one; the remaining step-1 work is the PLAYER only.** check-audio probe answers exists:true on 373 (Record flag → carrier → route, whole chain live). CAUGHT ON 373, FIXED + PINNED: Echo's greeting writing lost the race to the clip commit and the one-clock clamp filed it UNDER the question — the store's FIRST line through Echo's door now passes the ear-measured exception too (`!fromEcho || theirFirstLine == null` in staffSaid), later lines still clamp; rig scene asserts greeting-above-question AND answer-after-question (311 pass). Known harness reds on 373, both pre-existing: screen In-stock vs record not_in_stock (the 08-07 list fault) · word-for-word "uh" fillers (the 08-04 teach-the-matcher ruling). TRAP: Railway build stuck 15min "uploading snapshot" → deploymentCancel + serviceInstanceDeploy re-queued it. Suites: meter 49 · behaved 99 · call-events 109 · delta-clip 311 (2 reds = the pre-existing key-starved live-model scenes) · dropped-call 28 · bridge 13 · listen-nav 59 · tsc clean. Owner still decides where the last cents come from: the goodbye pause (own-brain plan) · the yellow meter · pricing. Then the next test on his word. **08-16: SELF-IMPROVING CHARLIE — owner approved the plan, REVERSED the 08-07 "money never fails a test" ruling, and stage one is MERGED TO STAGING + ADMIN SHIPPED on his "pull and go" (PR #121): `src/calls/meter.ts` (his color rule: green ≤23 · yellow 24-30 passes · red ≥31 FAILS · profit <67% FAILS) + `meter` bounds on TEST_CARDS (5 floor-exempt clock burners, his to re-rule) + v2For returns `test.meter` + the sheet's pill/red line/To pass read both halves; test-meter 25 · behaved 99 · tsc clean. Six-stage plan + approved UI homes: docs/specs/self-improving-charlie/README.md. **08-18: THE SIMULATIONS SIDE merged on his word** — `src/calls/simulations.ts` (grader = the SAME `TEST_CARDS`+`meterVerdict` by object identity, asserted in test-simulations 18) + `sim_runs` table (schema+bootstrap, own store, never call_results) + 3 admin routes + the Real checks | Simulations switch in app.html (real side untouched) + `scripts/simulate.ts` poster + `drive-simulations-local.ts` (refuses a real DB). Wording is SIMULATIONS never pretend calls, call N of M, no money numbers, verdict line names the robot store.** **08-17 EVENING, THE OWNER'S FIVE-PART BOX, ALL BUILT AND MERGED, THEN CHECK 374 DIALED ON HIS ORDER.** (1) THE PLAYER: `#v2audio` card in checkV2Html sits ABOVE the check log (the head speaker is gone), drag bar (`.v2scrub`, carved track + green fill via `--p`), a 15s pill, play/pause, total length; wired in openTestReceipt off the probe, hidden when no recording exists (proven on 370: section present, display none). THE DRAG NEEDED OUR OWN SLICING: passing the browser's Range to Twilio does NOTHING (it answers 200 + whole file, from the start AND the middle — the PM caught my first claim). `checkAudioBytes` fetches the mp3 once, holds 3 for 5 min, and the route answers 206 + content-range/accept-ranges itself. PROOF: bytes=0-65535 → 206, 65536 bytes, `bytes 0-65535/316500`; bytes=158250-223785 → its own 65536. Driven on the real Admin: drag → 0:40, 15s → 0:56, pause holds. (2) THE SET-ASIDE IS DELETED (his words: "we already have a system"): `holdCostAside` gone from MeterBounds + the 3 hold cards, the server's aside math gone, meter.ts grades `profitPct` against 67 only; test-meter 50. (3) THE TESTING PAGE'S BIG NUMBER = the newest check's id (`newest` on /api/admin/test-calls), forward only, no renumbering. Reads 374 now. (4) ONE SLOWEST-REPLY ROW: `oneSlowestReplyRow` (events.ts, used by BOTH receipt routes + the by-id one) keeps the biggest `gaps` stamp and gives it the closing words; the engine still stamps at every growth so the close race stays won. (5) ONE HAND-OVER EVER + LINE ENDS (`.unlock` for src/voice/** opened and DELETED): the ear's voice-stop now ARMS the hand through `armTheHand` (the same 300ms beat pieces already waited), and a joined line landing inside the beat is handed WHOLE, once (373 handed two turns 93ms apart). Every line files `endMs`: Echo's `forMs` rides echoHeardStaff→staffSaid→recordLine, our clips carry their own length, Charlie's line is stamped as his audio plays (`stampLineEnd`), and the answer gap anchors on `lastLineEndEpoch(room,"Clerk")` vs the ear vs the hand, whichever is LAST. Rig scene "CHECK 373'S SHAPE" FAILS on the old engine (2 turns, 2 steps) and passes here: delta-clip 319 · call-events 121 · meter 50 · behaved 99 · bridge 13 · dropped-call 28 · clip-stability 10 (new suite, in test-all.sh) · tsc clean · spec's 7 reds are the SAME 7 on b09f9289. (6) CLIPS ARE STABLE: the cache was memory-only, so every deploy re-recorded the line and ElevenLabs renders a different length each time (373's question 5.619s vs 372's 4.226s; hold ack 1.765 vs 1.625). clip-cache.ts now keeps bytes on the volume (`CLIP_CACHE_DIR` / RAILWAY_VOLUME_MOUNT_PATH/clips); no disk = old behaviour. First dial after the deploy re-recorded once at 4.3s and that is the length from here. **CHECK 374 (scene 5, room b92190dd): THE PILL SAYS TEST FAILED** — meter 35s (red at 31) + profit 55% under 67. Card half PASSED, not_in_stock right, every required row green, and all five fixes visible on the record: greeting is the FIRST line, ONE missed_turn at 74s, ONE slowest-reply row at 87s, profit graded on the real number, question clip 4.3s. Two known harness reds only (word-for-word "uh" fillers; screen In stock vs record not_in_stock, the 08-07 list fault). WHY THE METER GREW vs 373's 29s: the shorter question clip joins him ~2s earlier before the hold announce, and the tail after the comeback ran ~4s longer. **TRAP: Railway builds sat 15-20 min on "uploading snapshot"; I cancelled and re-queued twice and the OWNER RULED NEVER AGAIN — tell the PM, keep working, read /api/health for what staging really serves.** **08-17 LATE, THE FOUR FAULTS OFF 374, FIXED AND DIALED (check 375, scene 5, room 72cd99ee).** (1) THE COMEBACK WAIT, 5.8s→3.0s: 374's stamps split it as ear-stop 2.25s late (sinceVoiceStopMs 1208 at the hand, their line's own end 70.811) + Deepgram's first is_final 3.16s past the sound + 2.36s of provider generation. FIX: on a words-only reopen his session is already hearing them, so his OWN audio is let out (`HEARD_ENOUGH_MS` 1200 since `hisEarsBackAtMs`, new branch above the `!charlieMaySpeak` drop) — it emits `answered_from_his_ears`, clears the feed, marks pieces + `theirLastLine` his, and a late line whose sound overlapped his listening window is `markAsHis`, never handed (the new door above the 08-15 late-line hand). (2) NEVER MID GOODBYE: `hangUpAfterGoodbye` now polls until `hisVoiceOutAtMs >= askedAt-1.5s` AND `now >= agentPlayingUntil + 600ms`, capped 12s (2.5s when no sound of his ever came), then calls the old body, renamed `endTheCheck`, and stamps `goodbye_played_out`. (3) THE ROWS COUNT THE REAL SILENCE: the answer gap anchors on `lastLineEndEpoch(room,"Clerk")` when it is past `lastHoldEndAtMs`, else the ear; `lastHandAtMs` is OUT of the anchor (it hid 3.4s on 374); `sinceVoiceStopMs` on missed_turn uses the same anchor; his line files at `hisTurnAudioStartMs` (his SOUND), not when the provider's text arrived. (4) NO IMPOSSIBLE ENDS: his end waits in `hisEndWaitingForWords` until his own line is written (`hisTurnLineWritten`), because his sound starts before his words, which is how 374's hold reply got an end of 81.3s and his question 90.0s on an 88.0s call; plus `closeReceipt` trims every `endMs` to the call's end. Rig scene "CHECK 374'S SHAPE" FAILS on the old engine (his own answer dropped · no answered_from_his_ears · the words handed as a second turn) and passes here; the two goodbye scenes now play a voice and assert the line stays up while the goodbye is only words. Suites: delta-clip 330 · call-events 124 · meter 50 · behaved 99 · bridge 13 · dropped-call 28 · clip-stability 10 · tsc clean. **CHECK 375: PILL SAYS TEST FAILED** — meter 34s (red at 31) + profit 56% under 67, 11.1¢, not_in_stock, card half green. Their no ended 68.824 → his voice 71.815 (2.99s, the row reads 3s yellow) · handover 1s · drop 3s · goodbye 81.656→84.661 with the check ending 85.548 · hold ack 21.256→23.207 (1.95s, its own length). TAPE PROOF (ffmpeg via pip imageio-ffmpeg): 374's last 2.5s = 0,0,0,5,384 (silence then the goodbye's first burst) vs 375's 406,238,192,290,467 (his whole goodbye). Remaining fat = his own reply latency (~3s a turn) and 18s of quiet inside a 34s meter: the own-brain plan. **OWNER RULED 08-17 late: NEVER cancel or re-queue a Railway build again — tell the PM, keep working, read /api/health for what staging really serves.**
- **THE ROBOT CAN PLAY A RECORDING.** New `{ clip }` act + `ROBOT_CLIPS` (tapedeck.ts) + route `/robot/hold?f=<name>` (server.ts, NAMED never a path, behind the /robot/* switch). Clips are the owner's own, **committed, NEVER regenerated**; measured length + loudness live in `ROBOT_CLIPS` and are asserted in test-robot-store (rooms 11.2 / 13.9 dB under a speaking voice; `roomFraction` 0.35 = 9.1 dB is the bar). `03-hold-music-classic.mp3` peaks at 0.0 dB, LOUDER than a voice, which is why scene 20 uses it.
- **NEW SCENES 20 to 26:** 3 hold-music shapes (20 loud+even · 21 swells · 22 advert mixed in) · 2 rooms (23 quieter · 24 louder) · 25 Delta failed · 26 the runaround. **REWRITTEN: 10** (it could never test its own card — the robot moved us on before Charlie had a turn, so the ask that IS the card never happened on 332; it now states the wrong department and WAITS) and **12** (the 90s ring is the ring AFTER a transfer; ringing from the first dial is the carrier's no-answer and never reaches our engine). **26's HARD RULE: no single wait over 120s** (they are 84/55/60) or the hold cap ends it before the 4 minute cap; 205s of waiting carries it there. **THE SWEEP is a test now**, not a habit: every scene must end on a Staff line before the wait, with five named exemptions (6 · 9 · 12 · 17 · 26).
- **`deltaOff` IS A NEW CALL FLAG** (policy.ts + `ENV_FLAGS` + `KEEP_LOCAL_FLAGS`, all three). It short-circuits the ONE door in `charlie-setup.ts` that records the question, so the check takes the REAL fallback and not a second code path. Harness turns it on for scene 25 and back off in `finally`, same as `askForTransfer` for 16. **Note: bridge-place's timeline note still says "Could not record the opening question" on that check — src/voice was re-locked, and the sheet's own row reads right.**
- **`no_straight_answer` IS A NEW STATUS, NOT A RENAME.** Decider `keptAskingNoStraightAnswer` (service.ts, NOT verdict.ts: it decides nothing about stock, only the honest word). Needs BOTH: 2+ Clerk lines of 3+ words (we understood them) AND 2+ Agent lines with a "?" (we went back at it). Billable, and `CHARGED_ANYWAY` in credits.ts keeps billing and refunds from fighting. Scenes 13 + 16 expect it.
- **THE CARDS GRADE THEMSELVES.** `TEST_CARDS` gains `needs: BehavedKey[]` + `status`; `cardVerdict()` reads a check against them; `v2.test.verdict` rides the receipt; the sheet prints "To pass:", marks NEEDED rows and a Test passed/failed pill. **A named row the check never exercised is a FAIL, by name** (untested rows are hidden now, so a silent pass hides a broken engine). `delta_failed` deliberately does NOT need `question_recorded`. Money is banned from the rows and the ban is asserted.
- **THREE OLD ONES FIXED.** (1) The vague yes coin flip: OUR reader is settled (temp 0 + his 08-06 describe-it rule); the PROVIDER's extraction wobbles no/unclear, and no+our-yes was a contradiction. `reconcile` now lets a described product win, requiring ours=yes AND confidence>=0.75 AND a set/productForm the clerk named. (2) One check writing four rows: new column `part_of_check` on the fan-out rows + they are no longer listed or counted as checks. (3) **The Spanish Delta is wired**: both clips recorded before the dial (`openingClipEs`), and bridge picks at the clip commit off `theirFirstLine` through `staffSpokeSpanish` (`src/calls/staff-language.ts` — a SEPARATE judge from `guessLanguage`, which reads a MENU and answers "unknown" on a human greeting).
- **`scripts/alert-email-check.mjs`** is the one test that is not a scene: it arms nothing, refuses to dial when the alert is off or mail is not live, spawns `robot-check.mjs 1`, then proves ONE restock email left us with the mail service's own id. The inbox itself is the owner's step. **`.unlock` used ONCE for `src/voice/**`** (items 12 + 14 only) and once for `public/checkit.html` (the new status's Spanish); both deleted. **NEW TESTS** test-no-straight-answer + test-staff-language are in test-all.sh. Pre-existing red, NOT mine: `test-one-question` 2 FAILED on a clean baseline too.
- **WHAT THE NEXT AGENT CHANGED ON TOP, and it is right:** the three hold cards now grade a STATUS (a hold still ends in an answer, so a card ignoring it would go green on the wrong answer), scene 6 expects `left_on_hold` not Couldn't tell, and `listen-nav` grew a sound fingerprint for hold music and an advert talking over it, which is what scenes 20 to 22 exist to feed. **STILL OPEN, MINE:** bridge-place's timeline note reads "Could not record the opening question" on the Delta: failed check because `src/voice` was re-locked; the sheet's own row reads right.
## 08-07 ECHO HAS WORDS. `src/voice/transcriber.ts` = ONE Deepgram socket per check, opened on the **PICKUP FORK** (`/twilio-media` in server.ts, marked `?words=1`), which starts at pickup and runs the whole call. **NEVER the bridge's own stream: it starts later, misses the menu and the first words, and two sockets bill one call twice** (owner 08-07). That fork is `both_tracks`, so only `track === "inbound"` is transcribed or Charlie writes himself down as Staff; mapping's fork has no `words=1`. It reaches the check through `echoListening`/`echoHeardStaff` (bridge.ts); before the bridge socket exists a line still records, because the receipt opens at DIAL. **THE FLAG RIDES A `<Parameter>`, not the URL: check 353 ran with NO transcriber because the query never came back the way it was written, and it looked like an ordinary check. The fork logs which way it arrived, so a wordless check says so.** **PROVEN ON CHECK 354, scene 5: every Staff turn written, including "Next week, maybe. I'm not certain." at 172s with Charlie closed since 99s.**
- `timestamp`/`sequenceNumber` are read and the stream's zero ties to the wall clock once, so a line files at the second it was said. `staffSaid` is the ONE door: while Echo has the words Charlie's `user_transcript` no longer writes or relays the store's side (it still drives what he decides), and he gets what he missed as WORDS, so `heldWords` pacing is gone from the hold case. **STITCHED, 08-07:** `interim_results` on, `endpointing` 300, `utterance_end_ms` 1000, and the pieces are HELD until Deepgram's `UtteranceEnd` says the turn ended. NEVER flush on `speech_final` — that fires at the first BREATH and is exactly what split one sentence in two on 354. The bench runs the real transcriber now (`scripts/deepgram-bench.ts`, tsx) with 2s of quiet either side, 6/6 one line. **Check 354's silence traced and fixed: the live reader knocked signoff at 14s off the fragment "Pokemon?" at CONFIDENCE ZERO**, so Charlie was told to wrap up before Staff had gone to look; `live-read.ts` now needs `confidence > 0`. Charlie's ears were never the fault, the rig proves it (test-delta-clip, Echo scene). Check 355 (scene 2, 8.6¢) has him speaking twice and the turns whole. **EVERY 08-07 EVENING DECISION IS IN `docs/specs/testing-page/NEXT-ROUND.md` §10** (the signoff row naming its follow-up, ONE cost line per name opening to its pieces, `Echo (Ears)` + `Charlie (Voice)`, and `holdQuietMs` 6s to 3s still HIS CALL). **Delta replays ONLY after a real hand-over, never after a walk away**, and a reopen is triggered by their VOICE, not by Echo's words. **08-07 late, §11: all of it BUILT and proven on check 356**, plus `holdQuietMs` 6s to 3s, plus `hangUpAfterGoodbye` gaining a SECOND door because a goodbye said while the line was already going quiet left check 356 open from 59s to 145s and billed a third minute, plus `charlieMinOnLineMs` (5s, Admin) because 3 second quiet opened a session at 16s and closed it at 18 on 357. **SOLVED by the next agent the same night (`8c98d170`): a clock could never fix it, because the robot goes quiet the instant it finishes its line, so the quiet after Staff's answer is HIS thinking time, not them walking away. He is no longer dropped while he holds their answer and has not spoken in that session, capped at 6 seconds from the answer. `charlieMinOnLineMs` stays as the floor underneath it.** Cost: `sttPerMinUsd` 0.005, its own `Echo (Words)` bucket, `cost_stt_usd`, ~1.3¢ on a 158s check.
- **Delta is ONE CLIP**, Charlie prewarms behind it. **`prewarmTimer` MUST stay OUT of `clipTimers`** or a short clip leaves NO agent. **NO VOICE = NO CHECK.**
- **HOLD: `reopen` ALWAYS runs**. **THE FLAP IS DEAD** (receipt 199: 10 false transfers on a direct dial). ONE 20ms frame declared a transfer; BOTH edges need a RUN — `transferToneMs` 600 (a real burst is 2000) · `backVoiceMs` 400, in `tuning.ts`. **A transfer ALSO opens `hold_start`**; **`charlie_join` fires ONCE**. **07-31 TIMER AUDIT: NOTHING ends a mid-check hold** — only `maxCallSeconds` (300 on staging, ridden to Twilio as `timeLimitSec`). `holdMaxSeconds` (Admin "Hold max seconds", 60; code fallback 45) is **NOT a hang-up** — it OPENS Charlie when no human was ever heard. `ivrMaxSeconds` (90) is **WIRED TO NOTHING** — a dead Admin box. `ringMaxSeconds` 35 is the ONLY real give-up. **DECIDED 08-01: hold cap 120s, and `holdMaxSeconds`/`ivrMaxSeconds` are DELETED — see the Charlie record.**
- **§7 BRAIN — BLOCKED BY *THEM*, PROVEN 07-31, NOT OUR CONFIG.** custom LLM + an INSTANT clone is refused on agent CREATE **and** UPDATE (`custom_llm_not_allowed_in_with_agent_with_ivc_voice`); the SAME setting is accepted instantly with a `premade` OR `professional` voice — so it is the VOICE, nothing else. Branson HD is `cloned`, `is_allowed_to_fine_tune:false`. Plain `/v1/text-to-speech` with it returns audio (200), so building the talking ourselves keeps the voice. Creator plan: `professional_voice_limit:1`, `voice_limit:30`. **Owner emailed them to lift it**; else PVC (30 min + the real speaker verifying) or we own the conversation. Only route NOT behind the admin login; `..._OURBRAIN_AGENT_ID` unset.
- `call_dropped` is **never written `completed`** (the only status the one-hour block matches); Charlie bills as the SUM of open stretches. **`rollupFromRow` is the ONLY reader of a stamped call row**, both receipt routes use it.
- **ONE QUESTION, BOTH LANES.** `declaresOneTurn` (tapedeck): an EMPTY `followups.type` = set+format folded into the `set` line. Branson Global's lines say **WORD FOR WORD** or the agent rewrites them and loses the day.
## 08-06 THE ROBOT STORE: **ALL 19 SCENES ARE BUILT.** `docs/specs/robot-store/scenes-needed.md` is THE list: all 17 owner cards, every script owner-approved word for word, plus cards 18 (one exact product, arrives with hobby stores) and 19 (hold music with an advertising voice, the most dangerous hold: a recorded voice is the closest thing to Staff returning that is not Staff). The six that stopped one turn early (2, 3, 4, 5, 8, 10 — Staff answer, Charlie asks his follow-up, the robot had nothing left, no goodbye) are fixed, and the nine cards with no scene are written. **ALL 19 DIALED except 6, 7 and 9; every verdict + photos in `robot-run/walk.json`.** BATCH TWO (the holds) IS NOT BUILT; every clip is owner-approved by ear and COMMITTED at `public/robot-clips/` (do not regenerate). Volume against the Staff voice is the whole test on the room scenes; `ffmpeg` comes from pip `imageio-ffmpeg` when there is no system copy.
- **THE APPROACH WAS WRONG AND THE OWNER STOPPED IT (08-06 07:15, PM's fault).** The full-send order said dial every scene and report at the end — **a survey, not a fix**, so Echo kept moving on from broken scenes. **NEW RULE: ONE scene at a time — dial, read the record, fix, dial the SAME scene again, and nothing else dials until it holds clean.** Two scenes per order, never a list. **Echo has pushed NOTHING since 05:40**, so every scene dialed after that (13, 16, 17 at least) exists only in his chat and cannot be checked from the repo: **a run that is not pushed did not happen.** THREE FAULTS OFF THE OWNER'S OWN SCREENS, none of them fixed: (1) scene 13 (Staff never answers) — **Charlie asked the stock question THREE times** in three wordings, because the never-repeat rule only bites once an answer is IN HAND, and this scene never gives one; the verdict came back **Sold out off a call where stock was never mentioned**. (2) Scene 16 — **the signoff marker fired at 8s** ("Charlie understood the stock answer and was told to say goodbye when done") when Staff first spoke at 20s, so `nudgeSignoff` was knocked by a live read taken off the GREETING; every row downstream then read green on a check that should have failed, and Charlie asked when they were getting more in though nobody said they were out. (3) A hold dropped and reconnected Charlie INSIDE one Staff sentence — "Not usually." was written as "Not" (50s) then "Usually." (62s), and Delta replayed the question in between, which is why the customer screen showed the opener twice. **NOT A BUG, owner told: the verdict tail sharing the check's last second is deliberate (`receipt-store.ts` clamps it past the last row so it can never draw before the goodbye) — it reads as impossible on his sheet, so it is on the issue list, not fixed.**
- **PROVED BY ACCIDENT 08-06:** mapping had never wired Delta in, so Charlie asked the stock question himself, which is the designed fallback for a clip that never plays, and it WORKED. And a server restart mid test did not lose the run, the resume safeguard picked it back up on its own, the first time an unplanned restart has proven it.
## SWITCHES + WHAT WE MADE IN THE ACCOUNTS. Do not delete these wondering what they are. `cheapBridgeAll` **THE BIG ONE: OFF = every check takes the old path. ON for STAGING**, prod OFF till a promote · `ourBrain` blocked §7 (**Charlie on Anthropic API**) · `askForTransfer` ON. **`stopKeysOnHuman` + `closeAgentOnHold` ARE NO LONGER SWITCHES**: forced true in `getPolicy` AFTER the merge, because both saved blobs carry false. **Every guessed number is in `src/calls/tuning.ts`.** Agent `agent_2301kyk2rwgyfg8r50xk9enqwy2r` = staging `ELEVENLABS_MIDCALL_AGENT_ID`, **PROD NOT SET**; `check_brain_key` ↔ `BRAIN_API_KEY` (staging only). **`ENV_FLAGS` (app.html) must equal `KEEP_LOCAL_FLAGS` (settings-sync)** or the prod mirror stomps a staging test inside a minute; a spec check compares the lists. **A NEW call flag goes in BOTH.**
## THE WRONG-DEPARTMENT SAVE: SHIPPED 07-30, `askForTransfer` DEFAULT ON, Calls ▸ App (comp 1i), rule in `prompts.ts` on `{{ask_for_transfer}}`, BOTH lanes on the ONE switch. **WORDS, never the Ear (§10)**: `heardWrongDepartment` → `unknown`+`wrongDepartment:true` → `learnFromReceipt` + decay. **A hand-over is ALWAYS a new person** · the gap note must survive a reopen · a SILENT hand-over is caught because he ASKED (`askedToBePutThrough`, ONE copy, bridge AND scorecard). Gates **O.5 · O.6 · O.7**. `heardWrongDepartment`: the hand-over word is REQUIRED (bare `put you` matched "put you on hold"); a bare counter greeting ("Pharmacy, this is Joe") counts; `handingOver` is worked out ONCE and rides EVERY reason, and STAFF moving us marks the next wait a hand-over without us asking.
## 07-30 TESTING = CHARLIE BEHAVIOR, **THREE rows, ONLY Charlie**: Meter stopped · Transfer requested · Re-asked after transfer. DELETED, do NOT restore: `asked_once` · `mapping_held` · `no_keypad_at_person` — **OWNER RULE: walking a menu is not a test; the check failing IS the report.** A row belongs only if a WORKING check could hide it from him. Copy ASSERTED in `test-behaved.ts`. **`callSeconds` is OLD-path only → `rollupFromRow` reads the last step.** **PM: promote wanted — the engine + Testing's server half are staging-only.**
## 08-01 THE CHECK-LIFE FIX IS LIVE ON STAGING (PM-verified, both rounds). **GATEKEEPER `src/calls/check-life.ts` + table `check_life`**: alive/verdict questions answer from OUR record + the carrier, state in the DB (hard cap 30 min = lost-callback backstop, NEVER a call timer). Mirror = `setLifeHook` (events.ts, pure); the carrier's end is ALSO stamped directly in `/twiml/bridge-status`, the only witness after a restart. **EVERY finalize asks `isCheckAlive(row.room ?? row.providerCallId)` — never the provider, never with nothing.** The room is minted and written on the row BEFORE the dial on EVERY path (`placeBridgeCall opts.room`), or the gates ask about null and charge mid-hold. **TRAPS: the `direct:` lane is the provider's own line so the gatekeeper defers to it there; a pre-written row needs its `roomFinalizers` hook or an unanswered check sits on `dialing` forever; `normSaid` dedupes at record/relay/page and the page repaints only on change.**
## 08-01 RUN 6's THREE FAULTS, ALL FIXED + SHIPPED (his screenshot: one line reading "Hi, do you recall Fun Store? This is Bob. Um, I'm sorry, we don't today"). **THE ROOT: held audio was handed over in ONE BURST. The transcriber decides where a sentence ends by hearing a pause pass on a REAL clock, so a burst has no pauses in it at all — wrong words AND two turns welded into one. Injecting silence into a burst does NOTHING (shipped twice, changed nothing).** Fix: Charlie's EARS open when a person is found and the line flows live; anything still held is paced at 20ms a frame and catches up when the backlog is small. His MOUTH stays shut until the question ends, and that gate is ALSO what picks the joining agent. `preRoll` keeps the line CONTIGUOUS once a voice starts (keeping only loud frames deletes the pauses INSIDE a sentence and slurs it). The page takes our saved order once it holds as many LINES. **`scripts/what-happened.mjs` reads a REAL check's record — run it FIRST on any transcript fault (RULES 12).**
## 08-02 THE ROBOT STORE IS LIVE ON STAGING AND ALL TEN SCENES HAVE RUN FOR REAL (spec `docs/specs/robot-store/README.md`; checks 235-252). **It is the SAME tape deck pointed the other way** — inbound scenes live IN `src/calls/tapedeck.ts` (`robotAnswer`/`robotStep`, ten scenes + five greetings VERBATIM from `how-staff-actually-talk.md`), clips ride `clip-cache.ts` (`mp3Clip`, recorded ONCE), TwiML + audio on `/robot/*`, scene chosen by the `robot_scenario` setting ("7" or "7:2" pins the greeting) through `/api/admin/robot-store`. **NEVER write a second engine.** Number +1 424 484 7395 → `/robot/answer` on the staging service; MVP store 106362 points at it on BOTH envs (phone is on store-sync's never-sync list) and chain 121 is `ringsDirect` or the site greys it "Coming soon". Fun store untouched. Harness `scripts/robot-check.mjs` drives the REAL staging site, signs in as the owner (code 000000), and reads through `what-happened.mjs` (ONE reader). **TRAP: this sandbox's browser cannot open an encrypted connection at all** — the harness pipes it to the real site over plain HTTP; on a normal machine it goes straight there.
## 08-01 EVENING — THE CHARLIE RECORD EXISTS: `docs/specs/charlie-behavior/README.md` is now THE law for how Charlie behaves (every log line, every pass/fail both sides, the money boundaries off real rates, his eight tests which had NEVER been written down and died with each chat). Work orders beside it, owner-approved line by line: `echo-build.md` (round 1, running) + `echo-build-round-2.md`. **NOTHING SHIPS TO STAGING UNTIL THE ROBOT STORE CAN RUN A CHECK** (owner's own idea: a second number that answers itself, so an agent tests instead of his phone; it MUST vary across his eight tests, and a run gets a HARD CAP on how many checks it dials). Caught writing it: **`holdMaxSeconds` OPENS Charlie to an empty line at 60s** (his own log, real money) · a phone put down on a counter is neither quiet nor music so Charlie keeps billing (fourth shape: a loud room) · `ivrMaxSeconds` is a dead Admin box · the machine-phrase check can still hang up on real Staff BEFORE any hold. Three numbers move to Admin ▸ App and MUST live in `call_tuning`, never `policy_json` (prod's policy stomps staging every 60s): Charlie talk 23s · hold cap 120s · silence-before-drop 6s (keep 6 until the robot store measures the floor: too long costs ~1.1¢ a drop, too short makes him choppy and choppy loses whole checks).
## 08-02 CHARLIE ROUND 2 — **MERGED TO STAGING** (`echo-build-round-2.md`; PM audit failed the first cut, all items fixed). **`holdMaxSeconds` DELETED, not shortened** — it never ended a wait, it OPENED Charlie to an empty line at 63s (his log). Gone from `policy.ts` shape + defaults too, so it cannot be set, persisted or synced prod→staging; `ivrMaxSeconds` (wired to NOTHING) the same. **WHO ENDED THE CHECK = subtraction**: `noteWeEnded`/`weEndedCheck` stamped at ALL our endings (dropCall · voicemail · both give-ups · ring give-up · Charlie's close · `/pub/bridge-hangup` · `zoneHangRoom` · `hangupTwilioCall(callSid, room)` · Admin's stop); `/twiml/bridge-status` writes "The store hung up on us" / "The check was disconnected" when it was not us. **`farEndGone` is set on Twilio's `stop` FIRST** (real Twilio sends it on a far-end hangup while our leg still reads open). **OUR OWN `timeLimitSec` rides into the bridge**: a stop at/after the cap is `time_cap`, measured FROM THE DIAL (the bridge's own socket opens later, much later behind a menu), 3s slack — else our accounting reads as the store hanging up.
- **VOICEMAIL GATE = THE STORE'S FIRST LINE ONLY (`storeHasSpoken`). OWNER RULING, README §8 item 6, DO NOT RE-LITIGATE.** Not the spec's `humanAtMs`: a voicemail's recorded voice is what TRIPS the person detector, so that gate deletes the bail on its one real case; the earlier `weHaveAsked` gate had the same hole via the clip. Both fail because "a person answered" cannot yet tell a person from a recording — **round 1's person test fixes that, then KEEP BOTH.**
- Item 4: `looksLikeAMenu` (prompts.ts — words, never the Ear) + disconnected-during-transfer. Item 6: `mayWriteVerdict` + `lineStillUp` deleted · `/pub/charge` ONE capped list, survives a restart · `warnIfCapTooLow` (void) · the delta-lane thinness stated, not implied. **FOUND + FIXED: the gatekeeper answered "not alive" whenever the DATABASE was unreachable** (every lookup throws → unknown name → permission to stamp a verdict mid-check); a check we hold is now answered from memory first.
## 08-05 THE OWNER'S APPROVED REWRITE IS LIVE ON STAGING (`docs/specs/charlie-behavior/instructions-proposed.md`, approved 08-04 + 08-05, word for word). 21 sections → 14, **16,807 chars → 4,371** (joining 5,071). **INSERT OR NOTHING**: kiosk + wrong department + the store note + the specific product are all built values now (`kioskNote` / `departmentNote` / `specificityClause`), never flag prose; **the category is substituted IN CODE, never left as `{{category}}` inside a value** (a value is NOT rescanned for variables, in the provider or in `previewStorePrompt`, so a nested one reaches Staff as those characters). **RETIRED**: `{{kiosk_mode}}` `{{ask_for_transfer}}` `{{phone_tree}}` `{{other_categories}}` `{{voicemail_policy}}` `{{retailer_name}}` `{{location}}` `{{ask_shipment_day}}` `{{premium_followup}}` as spots (the last two vars still RIDE, nothing reads them), and `PREMIUM_FOLLOWUP` / `FREE_NO_FOLLOWUP` / `ASK_SHIPMENT_DAY` / `oneTurnFollowup` / `oneTurnShipmentDay` / `finderIsPremium` as code: the paying-vs-free split is dead. `{{set_example}}` reads `data/pokemon-sets.json` for the newest set ALREADY RELEASED (`setExampleFor`, 5 min cache; future sets ship into that file early, so "newest" alone would name one no store can have). **BOTH CHARLIES PULLED BACK AND COMPARED: identical, `scripts/verify-charlie-words.ts` (new, read-only, re-runnable).** Driven: prompts 185 · one-question · workflow-truth · delta-clip 232 · behaved 79 · tsc clean. **08-06 OWNER APPROVED FOUR MORE COPY EDITS, IN THE DOC, NOT YET IN `prompts.ts` (locked; the engine agent copies them word for word + updates test-prompts in the SAME commit).** §3 names the VAGUE YES ("Staff might not always say the word yes, and often answer vaguely by describing what they see"; "it's black boxes" / "we've got the little packs" / "just the tins" = YES). §10 and §11 ask ONLY for the piece Staff have NOT given, so half an answer never buys a second question: both given → wrap; package given → ask just the set; set given → ask just the package; neither → both in one sentence. Restock: day+time → wrap; day only → ask just the time; "soon" only → ask day+time once more, never a third. §12 bans correcting himself out loud (his test check said "Wait, you already mentioned black boxes, so that's helpful, do you happen to know the set name at all?" ALOUD). Placement rule the owner held: the vague yes lives in §3 (what counts as a yes) or it fights §7's sold-out NO.
- **HE ANSWERED THEIR HELLO ON EVERY CHECK. FIXED (`charlieMaySpeak`, bridge.ts).** ROOT, off EL's own record of 282-286: Staff's hello is the FIRST user turn the joining session gets, because it is exactly the preRoll+pending audio we hand over when he connects. A hello is a question, so he greeted back and re-asked ours. Whether the store HEARD it was clip length only: `charlieGateOpen` drops his voice while the clip PLAYS, so 285's 5.3s clip killed it silently (check read clean) and 286's 4.1s clip let it out. 282 and 286 also RECORDED it, because the `clipEchoDropped` fuzzy drop only fires on a word-for-word restatement. **Words could never fix this: `JOINING_RULE` has said "do NOT ask the question again" throughout and he did it 5/5.** THE GATE: `holdHimForTheirAnswer()` at all three clip commits (opening + `startOpeningClip` + the hand-over replay) shuts a SECOND gate; `letHimAnswer()` opens it on THEIR ANSWER. His audio is DROPPED, never queued (a delay just moves the duplicate later), and `agent_response` returns early while either gate is shut so a line the store never heard is not written down, relayed, or fed to the reader either. **287 PROVED THE GATE AND KILLED THE TIMER:** the held turn is on the record at 12s exactly as designed, then the 5s wait ran out at 16s and let him re-ask an answer given at 13s — the provider runs `patient`, so "Yeah." spoken at his 4s did not finalise into a LINE until past 7s. **So the EAR opens his mouth**: `ANSWER_VOICE_FRAMES` 15 (~0.30s), BARGE_THRESH while our clip plays / VOICE_THRESH after, ringback excluded, leaky count; the words path stays as the second opener; `ANSWER_WAIT_MS` is 9s and is ONLY the truly silent line (it must clear the provider's 5s `turn_timeout`, never race it).
- **THE WALK, one scene at a time (`ADMIN_TOKEN=… node scripts/robot-check.mjs <n>`), rule 15 binds: the owner reads each check before the next dials.** Scenes to cards, the nine cards with no scene yet, and every landmine: `docs/specs/robot-store/README.md` + `docs/specs/testing-page/NEXT-ROUND.md`. Reader: groq:llama-3.1-8b-instant, STATUS_READ_USD 0.000055. Record: `call_results.transcript_timed`, uncapped.
## 08-03 THE TESTING SECTION — **ELEVEN ROWS LIVE** (`behaved.ts`, README §5 is the law). New keys: handed_to_charlie · question_recorded · warmed_up_in_time · right_department · goodbye_when_told_no · wrapped_up · spoke_their_language · charlie_ended_the_check. **RENAMED to the record:** Transfer requested → "Asked to be transferred", Re-asked after transfer → "Reacted to a new person". Three states printed as WORDS + a count line. **Row 4 never ticks when the department was wrong** (it hands the verdict to row 5). **Row 6 names the event** (after a transfer / after a wait / a wait nobody came back from). **RETROACTIVE:** `beforeWeWroteItDown` (no `detail.step` anywhere) → "This check ran before we started writing that down"; row 9 falls back to `signedOff(last line)`. Receipt now also returns `lines` (the conversation) on BOTH routes; the sheet draws it under the ladder with the one `bubbles()` renderer. ONE engine addition the card needed: `saysNobodyToTransfer` (prompts.ts) + the `nobody_to_transfer` step, only read AFTER he asked. **DRIVEN:** behaved 69 · `scripts/qa-testing-card.mjs` on the REAL Admin (token → cookie by curl, every browser request node-fetched, `redirect: follow` or the sandbox proxy 503s).
## 08-03 CLOSEOUT (owner, five items, in the same branch). **NO COUNT OF RINGS EVER ENDS A CHECK** — `RINGS_UNANSWERED` deleted, `ringWaitTimer` armed at `firstRingAtMs`, cleared in `triggerConnect`; `ringWaitSeconds` 90. **`maxCheckSeconds` 240 replaces `pol.bail.maxCallSeconds`** as the check length (bridge-place `capSecs` → ctx + TwiML TimeLimit; callers pass `v.maxTalk ?? undefined`; slot TTLs follow it). **`departmentName`** = last spoken word of `opts.say`, read in bridge-place BEFORE the route is consumed → "Transferring to front" / "Transferring you to the Staff."; "desk" is gone. **THE TWO CHARLIES: `JOINING_RULE` + `joiningPrompt` + `midCallAgentPatch(llm)` live in prompts.ts, imported by service.ts AND make-midcall-agent.ts**; every `pushPrompt` now writes the joining agent too (he was frozen 07-28, missing the whole `{{ask_for_transfer}}` section — 17,521 vs 16,807 — which IS the asking-twice fault). Drift alarm in test-prompts. **FOUND DRIVING IT: the tone window survived a whole ring cadence**, so a person answering in the gap was judged against a mostly-ringing window and never opened Charlie — `BURST_END_FRAMES` 25 clears `loudE/loudT/voiced` after half a second of quiet. Numbers in Admin are FIVE.
## 08-04 LATE — THE TAIL RIDES EVERY DOOR + THE SIGNOFF'S REAL BLOCKER. **Three doors settle a verdict (sweep · the on-demand settle a WATCHING customer triggers · the webhook) and the watcher's door wins on every robot check** — the tail (second_read · decidedBy on the verdict · charged LAST) now rides all three from ONE recorder that takes the race (first door writes, others see the verdict and stop). `lastClerkLine` is the ONE decided-by. Proven live on check 276: second read at 31s · verdict in_stock · Customer charged, and the sheet drives ALL PASS on it (9.7¢ · 1:24 · 31s · 61%, five buckets, Status 0.0¢ is the read's honest rounding). **THE SIGNOFF'S BLOCKER: the reader's model dying mid check** (gemini 429 → fallback; a failed read + Staff never speaking again = no read at all). Whole chain proven in ONE process on the rig (line → reader → stubbed model at the same fetch → knock → note); live fix = ONE bounded retry 4s after a failed read, never a loop. **A CLICK COULD DEAFEN THE EAR** (270-272): the room test's yardstick was the loudest frame ever heard; one click pinned it and every voice after measured as the room, so Staff coming back were NEVER heard; now the MEDIAN of recent frames (`listen-nav` closeLevel), proven on the rig. **ADMIN SHELL TRAP: Mapper's ship-admin at 05:33 served an older shell over mine — ship-admin re-run; check the served stamp before believing a screen.**
- **WRITTEN DOWN, NOT MINE:** gemini flash-lite quota is EXHAUSTED (every verdict read 429s onto gpt-4o-mini) · the deployLogs API truncates to ~25 lines, useless for archaeology, `/pub/bridge-debug` is the real witness · **OWNER RULED 08-04: "151" IS the product's name and the written record is CORRECT** — the harness's word-for-word row is red because the robot's own script line says "one fifty one"; teach the WORD CHECKER (or the script line), never the transcript.
## 08-04 THE TESTING SCREEN + ITS ENGINE (this session, ALL ON STAGING, Admin shipped). **THE 16 LOCKED CARDS** live in `TEST_CARDS` (behaved.ts), word for word from `testing-cards.md`, asserted in test-behaved; each robot scene names its card and the dial stamps `named_test` on a robot-number check (bridge-place). **THE SHEET**: every Voice ▸ Testing check renders through `checkV2Html` off the real record (`checkV2From` in app.html builds the shape; the mock + `#mockcheck` are DELETED). **TRAP THAT BIT: the mock deletion measured its cut mock-to-hook and took `checkV2Html` with it** — restored byte for byte from the prior commit; only three wiring hooks differ (tile colors from the record, all three status tones, optional workflow bubble). **FIVE COST BUCKETS = `costBuckets` in cost.ts**, sum EXACTLY to total + `STATUS_READ_USD`; Bravo = the nav slice of line+listening; free buckets hidden. Receipt route returns `v2` (card · buckets · profit vs `perCallCents` · workflow bubble via store→chain→default). Verdict tail now writes `second_read` (model, cost) · `decidedBy` on the verdict · `charged` LAST (receipt-store recordVerdict extras).
## 08-04 ENGINE: **SIGNOFF** (`signoffDoors`/`nudgeSignoff` in bridge; live-read knocks on a definitive read; ONE note, never a hangup) · **NEVER REPEAT** line in RESTOCK_PROMPT (one source, drift alarm) · **DELTA REPLAYS AFTER A TRANSFER** (endHold `handedOn`: pendingClip again, gate shut, `clipEchoDropped` reset, the joining Charlie picked, note says the recording owns the question; recordLine gained `certain` so a clip WE played is never eaten as an echo) · **staff_hung_up** status (stamped like left_on_hold via `staffHungUpOn`, charged, ES label) · **PING CRASH**: pong on the connection it arrived on, replaced session ignored (bridge ~1157).
- DRIVEN 08-04 late: delta-clip **232** (two new signed-off scenes; the whole-chain stub now answers gemini in GEMINI's shape or an empty parse skips the fallback) · listen-nav 53 · behaved 79 · call-events 80 · prompts 142 · dropped-call 28 · bridge 13 · robot-store words held · tsc clean · ONE robot check per deploy (283 then 284), record read before every dial.
## 08-04 LATE — CHARLIE FINISHES A CHECK (owner's two-bug box, DONE, checks 283/284). **BUG 1 THE GOODBYE, TWO FAULTS:** (a) the signoff door hung when `room=""` — **the carrier's socket connects BARE, the name only arrives in the start message** — so the knock found nobody on EVERY real check while the rig (room handed in as a parameter) stayed green; `hangSignoffDoor()` now hangs at setup AND from the start handler, and the rig's whole-chain scene connects bare like the carrier. (b) The quiet after his goodbye read as Staff stepping away: he was dropped into a wait, the wait rule swallowed his session's close, the line sat open until the STORE hung up at 111s (282). `beginHold` now hangs up (`signed_off`) on quiet/room once the wrap-up was ASKED FOR **and** the goodbye is ON THE RECORD; music/transfer still hold, and the ear only counts quiet after our audio finished, so the goodbye is always fully out. Robot scene 1 now answers his follow-up (spec-approved line) or a goodbye is unspeakable by scene design. **BUG 2 NEVER TWICE — instruction at each leak's exact moment:** joining rule bans the question in ANY wording as his FIRST words · the wrap-up note bans repeating any earlier question once the answer is in hand · be-quick says an answer that does not fit is still their answer, take it and move on. Both Charlies verified carrying the new words at ElevenLabs after deploy. Check 284: Delta's question ONCE, first words the follow-up, goodbye 29s, ended 32s, "we never ask the same question twice" PASSES.
## THE ONE EAR — **a `PickupEar` was built here and DELETED the same night; §10 says never build a second.** A **VETO, never a green light**; anchors come off its own recording count. **PM: bail's else-branch blind-joins Charlie on a stopwatch if bail is off** (locked bridge). **`.unlock` WORKS**: the sprawl gate only hooks Write, so `printf 'src/voice/**\n' > .unlock` opens it; fix that scope ONLY, then DELETE it.
**HELD FOR THE OWNER:** live customer checks still fire recipes on elapsed seconds (menu-words nav built, defaults off) and their records call dial-to-person "nav" — flipping it changes the cost buckets, his call. The two whole-journey rig failures assert the OLD wait-for-approval behaviour R2 retires. **STILL SOURCE-TEXT (out of scope then):** voice-judge · map-sim · mapper-resume. **NOTED, NOT FIXED:** on a check walking a saved route the plan branch answers a prompt that reads as a question BEFORE the person judge is consulted, so Staff answering early on a mapped route can be pressed at — PM: decide in phase 3+.
**Phase 1, closed (08-01).** Charlie joins on the check's OWN record; graded when it ends, off his report. The department is his word (wrong department → redirect; otherwise Staff engaged → answered), never the in-stock reading. A mapping check NEVER takes a transfer (`neverTakeAHandover`, owner-named `src/voice/**` unlock, opened and deleted in that task) — customer checks still ride them, byte-identical. Seven practice checks written first and failing first; the leftovers (`GOING_TO_LOOK`, `SENT_AWAY`, `HUMAN_RE`, `LIVE_HUMAN_RE`, `looksLikeLivePerson`, `REDIRECT_RE`) are DELETED, and `navigator._test` lets a scripted check drive the real engine.
**Mapping phases 1 to 3 are CLOSED and their detail lives in `MAPPING-MANUAL.md` + git** (self-healing `src/calls/healing.ts`, the ring is evidence not an override, the chains screens, audit round 3). A mapping check never rides a hand-over: a good answer off a desk we cannot name or route to made mapping lock the wrong department.
## 08-08 THE KNOCK'S THREE FAULTS, FOUND BY DRIVING THE RED PRACTICE CHECKS (all fixed, on staging). The knock (press `123` at whoever answered) was built 08-07 and shipped with three holes nothing caught, because the practice checks were written before it existed. (1) **It returned BEFORE the store's line was written down**, so the opening line never reached the timeline and never reached `saidBefore` — the one test that catches a recording on a number we have never rung. It now sits AFTER the record is written, and never fires at a mailbox. (2) **`saidBefore` carried the line being judged**, so every line matched its own copy and came back "it has said this already" the FIRST time it was said: EVERY line on EVERY check read as a recording and no person was ever reached. `thisTurnLineIdx` marks the one step that is not evidence about itself. (3) **The knock's keys read as a route choice everywhere** — `pickedDoorFrom` marked "123" as the door we picked, `recipeFromCall`/`recordCallPath` wrote `press 123` into the store's SAVED ROUTE, `classifyMode` called a direct store Alpha, sweep's `acted` false-flagged direct chains, and `weSpokeAtSec` read a recording's next line as a reply to us. `NavStep.knock` marks it and all nine readers skip it. Practice check 8 pins all of it. **DRIVEN 08-08:** practice-checks **57** (was 38 of 49 red) · voice-judge 63 · listen-nav 59 · map-sim **367** · resume 21 · mapgraph 62 · map-api 20 · map-e2e 104 · recipe green · tsc clean. **TRAPS: a page-driving script that boots a server MUST be killed in the same turn** (I left eight running 45 minutes and the OWNER found them: `ps -eo pid,etimes,args | grep src/server.ts` before closing out) · **map-sim leaves `local.db` behind — `rm -f local.db` before a re-run or it dies on a UNIQUE constraint that looks like a real failure** · **a staging push RESTARTS staging** and kills whoever is mid test check · **merge MY branch INTO staging, never the other way** (I did it backwards on 08-07 and none of the work was live) · **STAY IN THE MAPPING LANE.**
- **THE THIRTEEN MAPPING TESTS ARE THE PM'S LIST**, `docs/specs/mapping-tests/README.md` (on staging 08-08). **ONLY ONE HAS EVER BEEN ON A REAL PHONE: Direct pickup**, the one mapping check to the Fun store the owner picked up himself — and that dial PREDATES the knock, so **the knock has never been dialed at all**; the other twelve are bench-only. The real CVS calls of late July (Anaheim · Alhambra · Mulholland · Lanett) are where several of these rules came from, but they ran before the hand-off to Charlie and before the knock, so they prove the rules, not today's code. **THE ROBOT STORE ANSWERS AS A PHONE MENU NOW (merged 08-14), built as a SCENE inside tapedeck.ts, never a second engine**: `label`/`goto` acts + a `menu` third voice + the key table ON the scene (`keys`/`keysElse`/`holdKeysUntil`/`swallowEarlyKeys`/`staffAt`); `/robot/step` takes `Digits` beside `SpeechResult`; the front desk splices the chosen Staff scene into the SAME call. All five owner variants (plain · no_option_fits · menu_changed with old key 0 to the pharmacy, his ruling · press_ignored · ring_out), words verbatim from `robot-menu.md`. OFF unless the `robot_menu` setting names a variant, so the walk's scenes are untouched. The knock is answered OFF THE SOUND now, three answers (read on = machine · stopped then spoke = person · stopped and stayed stopped = person waiting), words get no vote; a real stop must RUN 1.5s, bench-set, first real dial proves it. Bench 08-14 (PM re-ran, merged tree): robot-menu 66 · robot-store all held · practice 65 · tsc clean. **NOTHING DIALED; the mapping walk starts on the owner's word.**
## Engine — Branson HD `1P1JhCcLzeMmkvLi1BkG` speed 0.91 (BOTH envs) · echo gate 520/150 · ringback by published
tone frequencies, 6 rings = hang up · **Charlie $0.00183/s, meters SILENCE** · Twilio WHOLE MINUTES · 5.2c base ·
**all FOUR dial paths open a receipt** (bridge-place · navigator · tapedeck · native `direct:<id>`). Traps: never
run the full suite for a small change; never deploy mid test check; `/api/*` is admin-gated whole (a THIRD-PARTY
route must be exempted or it 401s silently); auto-nav 0-hammers when it cannot parse → FALSE "no human"; old
whole-call Delta STAYS in the tree.
