# data/ — store-data reference (owned by Check - Data Dev). Samples separated from docs.
| Doc | What it is |
|---|---|
| `provenance.md` | Where every piece of store data comes from — the source of truth |
| `store-schema.md` | How a store row is structured |
| `chains-list.md` | The 80 exact chain names (match these; avoid duplicate chains) |
| `scoring.md` | The Fungibles Score — how store tiers work (repo-native, reconciled) |
| `SCORING_MODEL_spec.md` | The v4 rubric **as delivered** (upstream source of `scoring.md`); CSVs stay in `data/source/chain-scoring-2026-06/` |
| `DEV_HANDOFF_final.md` | Import instructions for the chain-scoring package (CSVs live in `data/source/chain-scoring-2026-06/`) |
| `COVERAGE_REPORT.md` | Gaps in the stores-master dataset (datasets live in `data/stores-master/`) |
| `store-logos.md` | The retail-chain logo pipeline (Target/Walmart PNGs → the site) |
| `samples/` | Sample data files (.json/.csv) — data, not docs |
