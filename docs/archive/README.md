# archive/ — done or superseded. Nothing here is current; it's kept so "where did X go?" has an answer.
Agents: if a doc you remember is gone, look here first, then `docs/START-HERE.md` for the map.
Files are suffixed with WHY they're here (e.g. `-SHIPPED`).
